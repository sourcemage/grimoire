if [ "$DEV_CMAKE" == "y" ]; then
  cd ${SOURCE_DIRECTORY}/build &&
  make install
else
  default_install
fi &&

if [[ $WEECHAT_SYMLINKS == y ]]; then
  ln -s weechat-curses ${INSTALL_ROOT}/usr/bin/weechat
fi
