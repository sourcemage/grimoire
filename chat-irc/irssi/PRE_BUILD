default_pre_build &&
cd ${SOURCE_DIRECTORY} &&
if [ "$IRSSI_SVN" = "y" ]; then
NOCONFIGURE=yes ./autogen.sh
fi &&
if [ "$IRSSI_SVN" = "n" ]; then
  patch  -p0  <  $SCRIPT_DIRECTORY/irssi-0.8.10-dcc-segv.patch
fi
