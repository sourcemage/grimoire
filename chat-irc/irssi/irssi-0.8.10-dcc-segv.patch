--- src/irc/dcc/dcc.c.orig	2006-08-28 07:08:13.000000000 +0500
+++ src/irc/dcc/dcc.c	2006-08-28 07:09:17.000000000 +0500
@@ -58,8 +58,8 @@
 
 	pos = gslist_find_string(dcc_types, type);
 	if (pos != NULL) {
-                dcc_types = g_slist_remove(dcc_types, pos->data);
 		g_free(pos->data);
+                dcc_types = g_slist_remove(dcc_types, pos->data);
 	}
 }
 
