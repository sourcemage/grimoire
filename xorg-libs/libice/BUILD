OPTS="$LIBICE_OPTS $OPTS"
OPTS="${OPTS} ${XF86_INPUT_LIBICE_UNIX_TRANSPORT}"  &&
OPTS="${OPTS} ${XF86_INPUT_LIBICE_TCP_TRANSPORT}"   &&
OPTS="${OPTS} ${XF86_INPUT_LIBICE_IPV6}"            &&

default_build
