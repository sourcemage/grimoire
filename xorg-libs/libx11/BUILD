OPTS="$LIBX11_OPTS $OPTS"
OPTS="${OPTS} ${LIBX11_SRPC}"      &&
OPTS="${OPTS} ${LIBX11_I18N}"      &&
OPTS="${OPTS} ${LIBX11_CURSOR}"    &&
OPTS="${OPTS} ${LIBX11_XCMS}"      &&
OPTS="${OPTS} ${LIBX11_LOCALE}"    &&
OPTS="${OPTS} ${LIBX11_LOCALDIR}"  &&
OPTS="${OPTS} ${LIBX11_XKB}"       &&

default_build
