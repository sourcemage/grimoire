OPTS="${OPTS} ${XF86_INPUT_LIBFONT_TYPE1}"         &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_CID}"           &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_SPEEDO}"        &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_BUILTINS}"      &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_PCFFORMAT}"     &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_BDFFORMAT}"     &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_SNFFORMAT}"     &&
OPTS="${OPTS} ${XF86_INPUT_LIBFONT_FC}"            &&
OPTS="${OPTS} ${XF86_INPUT_LIBFS_UNIX_TRANSPORT}"  &&
OPTS="${OPTS} ${XF86_INPUT_LIBFS_TCP_TRANSPORT}"   &&
OPTS="${OPTS} ${XF86_INPUT_LIBFS_IPV6}"            &&

default_build
