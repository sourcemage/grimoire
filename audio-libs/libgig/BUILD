make_single                                                &&
OPTS="--disable-dependency-tracking $OPTS"  default_build  &&
make_normal                                                &&

if  is_depends_enabled  $SPELL  doxygen
then
  make  docs
fi
