depends  autoconf  &&
depends  automake  &&
depends  libtool   &&
depends  g++       &&

if [ "$XMMS" = "y" ]; then
  depends  xmms  '--with-xmms'  &&
  depends  id3lib
fi

if [ "$FAAD_CVS" = "y" ]; then
  depends  CVS
fi

optional_depends  libmp4v2              \
                  "--with-mp4id"        \
                  "--without-mp4id"     \
                  "For MPEG4v2 support"
