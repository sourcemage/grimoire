. $GRIMOIRE/FUNCTIONS
make install &&

# install the kernel module
if [[ $KERMOD_OPTS == --with-kmodule* ]]; then
    make -C ${INSTALL_ROOT}/usr/src/linux-$(get_kernel_version) M=${SOURCE_DIRECTORY}/kbuild modules_install
fi
