depends  alsa-lib  &&
depends  ladspa    &&

if [[ $DSSI_HOST == y ]]; then
  depends  liblo  &&
  depends  jack   &&
  if [[ $DSSI_PLUGINS == y ]]; then
    depends  libsamplerate  &&
    depends  libsndfile     &&
    depends  qt-x11
  fi
fi
