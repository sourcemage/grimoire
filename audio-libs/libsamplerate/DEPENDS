depends pkgconfig &&

optional_depends libsndfile \
                 "--enable-sndfile" \
                 "--disable-sndfile" \
                 "to build sndfile-resample utility" &&

optional_depends fftw \
                 "--enable-fftw" \
                 "--disable-fftw" \
                 "for FFTW usage"
