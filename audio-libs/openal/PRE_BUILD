default_pre_build                                       &&
if [[ $OPENAL_VERSION != release ]] ; then
cd ${SOURCE_DIRECTORY}                                  &&
rm -rf `find -type d -name CVS`                         &&
cd linux                                                &&
NOCONFIGURE=Y ./autogen.sh
fi
