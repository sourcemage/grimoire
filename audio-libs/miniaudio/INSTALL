install -m444 miniaudio.h -Dt "$INSTALL_ROOT/usr/include/miniaudio" &&
ln -sf miniaudio/miniaudio.h "$INSTALL_ROOT/usr/include/miniaudio.h" &&
install -m444 miniaudio.pc -Dt "$INSTALL_ROOT/usr/lib/pkgconfig"
