optional_depends libgcrypt \
                 "--with-gcrypt" \
                 "--without-gcrypt" \
                 "for MD5 computation" &&
optional_depends libftdi \
                 "--with-exsid" \
                 "--without-exsid" \
                 "for exsid support"
