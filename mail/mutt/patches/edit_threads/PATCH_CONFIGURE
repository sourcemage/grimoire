if list_find "${MUTT_OPTS}" "--enable-imap"; then
  config_query_option MUTT_OPTS "Enable thread editing for IMAP?" \
                                "y"                               \
                                 "--enable-imap-edit-threads"     \
                                 ""
fi
