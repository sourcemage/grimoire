message  "${MESSAGE_COLOR}Remember that the default IMAP port in pine is 220.${DEFAULT_COLOR}"

config_query  SQUIRRELMAIL_BACKUP  "Backup config on dispel?"              y  &&
config_query  SQUIRRELMAIL_ORDER   "Sort by lastname (instead of first)?"  n
