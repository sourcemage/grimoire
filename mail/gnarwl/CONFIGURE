message "${MESSAGE_COLOR}"
message "The following option is useful in query filter with vacationStart and"
message "vacationEnd attributes. It gives a number of days since Unix Epoch"
message "(1 January 1970)."
message "${DEFAULT_COLOR}" &&

config_query GNARWL_TIME "Do you want to patch in support for expansion of time variable?" n
