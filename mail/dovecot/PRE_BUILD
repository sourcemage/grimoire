default_pre_build                                   &&
cd $SOURCE_DIRECTORY                                &&

patch -p0 < "$SPELL_DIRECTORY/ssl_protocols.patch" &&

if [[ "$DOVECOT_PIGEONHOLE" == "y" ]]; then
unpack_file 3
fi
