default_pre_build                            &&
cd  $SOURCE_DIRECTORY                        &&

cp  mail/config/mozconfig  .mozconfig        &&

if  [ "$BUILD_ENIGMAIL" = "y" ];  then
  cd  $SOURCE_DIRECTORY/mailnews/extensions  &&
  unpack_file  3
fi
