default_pre_build &&
cd ${SOURCE_DIRECTORY} &&
mkdir -p MailRoot/var/log/xmail &&
mkdir -p MailRoot/var/spool/xmail &&
mkdir -p MailRoot/var/spool/temp &&
mkdir -p MailRoot/var/spool/local &&
mkdir -p MailRoot/var/run/xmail &&
mkdir -p MailRoot/etc/xmail/cmdaliases &&
mkdir -p MailRoot/etc/xmail/custdomains &&
mkdir -p MailRoot/etc/xmail/dnscache &&
mkdir -p MailRoot/etc/xmail/domains &&
mkdir -p MailRoot/etc/xmail/filters &&
mkdir -p MailRoot/etc/xmail/pop3linklocks &&
mkdir -p MailRoot/etc/xmail/pop3links &&
mkdir -p MailRoot/etc/xmail/pop3locks &&
mkdir -p MailRoot/etc/xmail/tabindex &&
mkdir -p MailRoot/etc/xmail/userauth &&
patch -p1 < ${SCRIPT_DIRECTORY}/xmail-${VERSION}-FHS-smgl.diff
mkdir MailRoot2 &&
mv MailRoot/etc MailRoot2 &&
mv MailRoot/var MailRoot2 &&
rm -rf MailRoot &&
mv MailRoot2 MailRoot
