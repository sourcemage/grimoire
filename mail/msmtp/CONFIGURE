config_query_list MSMTP_SSL "Which SSL backend do you want for TLS/SSL support?" \
                            gnutls \
                            openssl \
                            none &&

config_query MSMTP_SENDMAIL "Enable Sendmail mode?" y
