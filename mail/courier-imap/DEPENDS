depends SSL &&
depends perl &&
depends courier-authlib &&

if [[ "$CIMAP_DB" == "db" ]]; then
    depends db "--with-db=db"
else
    depends gdbm "--with-db=gdbm"
fi &&

optional_depends "fam" \
                 "" \
                 "" \
                 "to provide real-time folder status updates"
