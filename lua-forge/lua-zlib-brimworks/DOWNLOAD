if [[ ! -e "${SOURCE_CACHE}/${SOURCE}" ]]; then
  wget -c --no-check-certificate -O lua-zlib-brimworks-v0.2.tar.gz  https://github.com/brimworks/lua-zlib/tarball/v0.2 &&
  mv ${SOURCE} ${SOURCE_CACHE}
fi
