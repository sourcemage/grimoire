source $SECTION_DIRECTORY/KDE_DEPENDS
if [[ $KDN_SVN  == y ]]; then
  depends subversion
fi                      &&
#depends mlt -sub FFMPEG  &&
depends LIBAVCODEC      &&
depends	mlt  &&
depends shared-mime-info
