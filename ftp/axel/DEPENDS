optional_depends SSL \
                 "--with-openssl" \
                 "--without-openssl" \
                 "for TLS support" &&

optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
