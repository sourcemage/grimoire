. $GRIMOIRE/FUNCTIONS

vdepends << ! &&
	libtorrent >= 0.12.9
!
depends curl       &&
depends ncurses    &&

optional_depends  xmlrpc-c               \
                  "--with-xmlrpc-c"      \
                  "--without-xmlrpc-c"   \
                  "for XMLRPC interface"
