depends libtorrent &&
force_depends libtorrent &&
depends curl       &&

optional_depends  xmlrpc-c               \
                  "--with-xmlrpc-c"      \
                  "--without-xmlrpc-c"   \
                  "for XMLRPC interface"

