default_pre_build                                             
#dht patch, removed until new version
#cd ${SOURCE_DIRECTORY}                                        &&
#if [[ ${DHT_PATCH} != n ]]; then
#  cp ${SPELL_DIRECTORY}/dht.diff ${SOURCE_DIRECTORY}/dht.diff &&
#  patch -p1 < dht.diff
#fi

