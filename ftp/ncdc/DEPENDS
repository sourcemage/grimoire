depends ncurses &&
depends zlib &&
depends bzip2 &&
depends sqlite &&
depends glib2 &&
depends gnutls &&

optional_depends geoip \
                 "--with-geoip" \
                 "--without-geoip" \
                 "for IP-to-country lookups support"
