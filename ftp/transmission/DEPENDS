depends openssl &&
depends intltool &&

optional_depends libnotify \
                 "--enable-libnotify" \
                 "--disable-libnotify" \
                 "for notifications support" &&

optional_depends gtk+2 \
                 "--enable-gtk" \
                 "--disable-gtk" \
                 "for GTK+2 client"
