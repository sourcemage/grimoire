depends -sub CXX gcc &&

optional_depends LIBSASL \
                 "--enable-sasl" \
                 "--disable-sasl" \
                 "for sasl support" &&

optional_depends libevent \
                 "--enable-libevent" \
                 "--disable-libevent" \
                 "for libevent support"
