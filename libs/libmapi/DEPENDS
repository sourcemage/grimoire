depends  samba4  &&

if  [[  "$LIBMAPI_SCM"  ==  "y"  ]];  then
  depends  SVN
fi  &&

optional_depends  swig                       \
                  '--enable-swig-perl'       \
                  '--disable-swig-perl'      \
                  'for Perl SWIG interface'
