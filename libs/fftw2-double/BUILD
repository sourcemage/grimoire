cd $SOURCE_DIRECTORY     &&
./configure  --prefix=${INSTALL_ROOT}/usr  \
             --enable-shared               \
             --enable-i386-hacks           \
      $OPTS                                &&
make
