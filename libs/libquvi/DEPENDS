depends m4 &&
depends pkgconfig &&
depends lua &&
depends curl &&
depends libquvi-scripts &&

optional_depends libsoup \
                 "--with-soup" \
                 "--without-soup" \
                 "to build (examples) with libsoup"
