default_pre_build  &&
cd $SOURCE_DIRECTORY &&
bzcat $SPELL_DIRECTORY/virt.diff.bz2 | patch  -p1 &&
patch -p1 -d $SOURCE_DIRECTORY < $SPELL_DIRECTORY/librdf_storage_register_factory.patch &&
libtoolize --force &&
aclocal    &&
autoheader &&
automake --add-missing &&
autoconf
