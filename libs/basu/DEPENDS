. "$GRIMOIRE"/MESON_DEPENDS &&
optional_depends audit \
                 audit={enabled,disabled} \
                 "for libaudit support"
optional_depends libcap \
                 libcap={enabled,disabled} \
                 "for libcap support"
