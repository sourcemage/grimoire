default_pre_build                 &&
local FF                           &&
tar xzf $SPELL-*.tar.gz          &&
cd $SOURCE_DIRECTORY             &&
# apply multiple patches
#for FF in `ls ../*.patch`;do
#  patch -p0 < $FF
#done                              &&
# remove files
rm -f ../newt*{patch,gz,spec}
