default_pre_build                 &&
tar xzf  $SPELL-*.tar.gz          &&
# apply patches
patch -p0 < newt*.patch           &&
# remove files
rm newt*{patch,gz,spec}           &&
cd $SOURCE_DIRECTORY
