default_pre_build                  &&
cd $SOURCE_DIRECTORY               &&

# Bug 11663
sedit "65d" src/video/ggi/SDL_ggievents.c  &&

mkdir -p smgl                      &&
cp $SCRIPT_DIRECTORY/sdl smgl/     &&
cp $SCRIPT_DIRECTORY/sdl.sh smgl/
