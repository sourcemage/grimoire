default_pre_build                  &&
cd $SOURCE_DIRECTORY               &&
patch -p0 < $SCRIPT_DIRECTORY/no-yasm.patch  &&

mkdir -p smgl                      &&
cp $SCRIPT_DIRECTORY/sdl smgl/     &&
cp $SCRIPT_DIRECTORY/sdl.sh smgl/
