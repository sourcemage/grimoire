depends  gcc            &&
depends  gettext        &&
depends  gnupg          &&
depends  perl           &&
depends  smgl-fhs       &&
depends  smgl-archspecs &&

if  [  "$GLIBC_USEIDN" = "y"  ]; then
	depends libidn
fi
