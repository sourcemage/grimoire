cd $SOURCE_DIRECTORY &&
DEST=$INSTALL_ROOT/usr/include/re2 &&
mkdir -p $DEST    &&
cp re2/*.h $DEST  &&
DEST=$INSTALL_ROOT/usr/lib &&
mkdir -p $DEST    &&
cp build/libre2.so $DEST &&
DEST=$INSTALL_ROOT/usr/lib/pkgconfig &&
mkdir -p $DEST    &&
cp re2.pc $DEST
