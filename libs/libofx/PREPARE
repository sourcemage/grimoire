config_query LIBOFX_CVS "Use CVS for latest development version?"  n  &&

if  [  "${LIBOFX_CVS}"  ==  "y"  ];  then
  config_query LIBOFX_CVS_AUTOUPDATE "Update to latest CVS automatically on system updates?"  n
fi
