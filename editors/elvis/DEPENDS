depends ncurses &&

optional_depends libx11 \
                 "--with-x" \
                 "--without-x" \
                 "for a GUI editor" &&

if is_depends_enabled $SPELL libx11; then
  depends expat &&
  depends libxft
fi
