default_pre_build                                                 &&
if [[ "${VIM_CVS}" == "n" ]] ; then
  if echo "$OPTS" | grep -q "\-\-enable\-tclinterp"
  then patch ${SOURCE_DIRECTORY}/src/auto/configure                 \
        < ${SPELL_DIRECTORY}/broken_tcl.patch
  fi
fi
