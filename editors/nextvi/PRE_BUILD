default_pre_build &&
cd "$SOURCE_DIRECTORY" &&
seq 3 100 | while read n; do
  file="SOURCE$n"
  file="${!file}"
  [ -n "$file" ] || break
  printf '%s\0' "$SOURCE_CACHE/$file"
done |
  xargs -r -0 -n 1 -t patch -fp1 -i
