cd ${BUILD_DIRECTORY} &&
unpack_file &&
unpack_file '2' &&
unpack_file '3' &&
unpack_file '4' &&
if [[ "${OOO_BINFILTER}" = 'y' ]]; then
  unpack_file '5'
fi &&
if [[ "${OOO_TEST}" = 'y' ]]; then
  unpack_file '6'
fi &&
cd ${SOURCE_DIRECTORY} &&
patch -p0 < ${SPELL_DIRECTORY}/getline.patch
