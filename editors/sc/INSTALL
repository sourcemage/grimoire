# The Makefile fails if these directories do not exist beforehand.
echo "INSTALL_ROOT: ${INSTALL_ROOT}"
mkdir -p ${INSTALL_ROOT}/usr/share/man/man1 \
         ${INSTALL_ROOT}/usr/lib \
         ${INSTALL_ROOT}/usr/bin &&

default_install
