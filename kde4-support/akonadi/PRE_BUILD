default_pre_build    &&
cd $SOURCE_DIRECTORY &&
message "${MESSAGE_COLOR}Applying fix for boost 1.53+...${DEFAULT_COLOR}" &&
patch -p0 < "${SCRIPT_DIRECTORY}"/boost-1.53.patch &&
sed -i '/update_xdg_mimetypes/D' CMakeLists.txt
