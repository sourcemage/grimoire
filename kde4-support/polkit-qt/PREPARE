#config_query_list AK_VER "which version to build?" stable unstable devel  &&
persistent_add AK_VER  &&
AK_VER=devel  &&
if [[ $AK_VER == devel ]]; then
  config_query  AK_VCS_AUTOUPDATE  \
                "Update automatically on every system update?"  n
fi

