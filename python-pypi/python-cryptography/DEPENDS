depends python3 &&
depends pycparser &&
depends semantic_version &&
depends SSL &&
depends SETUPTOOLS &&
depends cffi &&
depends pyasn1

# Future versions may require these
#depends rust &&
#depends setuptools-rust
