depends python &&
depends pygments &&

suggest_depends curtsies "" "" "for Curtsies frontend support (${MESSAGE_COLOR}also requires greenlet${DEFAULT_COLOR})" &&

if is_depends_enabled $SPELL curtsies; then
  depends greenlet
fi &&

suggest_depends pygtk2 "" "" "for GTK interface support" &&
suggest_depends urwid "" "" "for console interface support"
