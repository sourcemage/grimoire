depends python3 &&
depends SETUPTOOLS &&

runtime_depends tcpdump &&

suggest_depends nmap "" "" "for fingerprinting support" &&
suggest_depends sox "" "" "for VOIP support" &&
suggest_depends pycryptodome "" "" "for WEP decryption support" &&

if is_depends_enabled ${SPELL} pycryptodome; then
  depends python-ecdsa
fi
