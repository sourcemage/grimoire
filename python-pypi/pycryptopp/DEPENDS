depends python &&
depends SETUPTOOLS &&
depends -sub CXX gcc &&

optional_depends cryptopp \
                 "--disable-embedded-cryptopp" \
                 "" \
                 "to build against system Crypto++ library"
