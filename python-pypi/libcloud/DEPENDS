depends PYTHON &&
depends requests &&
depends lockfile &&

suggest_depends pycrypto "" "" "for service account authentication at Google Cloud Platform and Microsoft Azure" &&
suggest_depends lxml "" "" "to use lxml for parsing XML (reduces parsing time and memory usage)" &&
suggest_depends certifi "" "" "for CA usage support"
