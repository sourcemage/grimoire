. "$GRIMOIRE/FUNCTIONS"						&&
depends qscintilla						&&
depends python							&&

if spell_ok qscintilla && is_version_less $(installed_version qscintilla) $VERSION; then
  force_depends qscintilla
fi

