[ -d ${INSTALL_ROOT}/usr/share/convirt ] || mkdir -p ${INSTALL_ROOT}/usr/share/convirt
cp -r ${SOURCE_DIRECTORY}/* ${INSTALL_ROOT}/usr/share/convirt/ &&
install -m 0755 ${SCRIPT_DIRECTORY}/convirt ${INSTALL_ROOT}/usr/bin/convirt
