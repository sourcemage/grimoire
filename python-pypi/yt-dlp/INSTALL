mkdir -p "${SOURCE_DIRECTORY}/builddir" &&
PYTHONPATH="${SPELL/-/_}" python3 -m installer --destdir="${SOURCE_DIRECTORY}/builddir" dist/*.whl &&

cd "${SOURCE_DIRECTORY}/builddir" &&
find . | cpio --pass-through --make-directories --unconditional --verbose \
              "${INSTALL_ROOT}/"
