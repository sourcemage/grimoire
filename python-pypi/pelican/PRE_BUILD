default_pre_build        &&
cd "${SOURCE_DIRECTORY}" &&

patch -p1 < "${SPELL_DIRECTORY}/0001-initialize-locale-fixes-2043.patch" &&
if is_depends_enabled $SPELL python3; then
  patch -p1 < "${SPELL_DIRECTORY}/0002-use-python3.patch"
fi

