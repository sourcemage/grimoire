mkdir -p "$INSTALL_ROOT/usr/share/mplayer/skins" &&
cp -av Blue "$INSTALL_ROOT/usr/share/mplayer/skins" &&

ln -vsf "$TRACK_ROOT/usr/share/mplayer/skins/Blue" \
        "$INSTALL_ROOT/usr/share/mplayer/skins/default"
