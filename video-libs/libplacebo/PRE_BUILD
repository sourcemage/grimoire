default_pre_build &&

if [[ "${LIBPLACEBO_BRANCH}" == "scm" ]]; then
  # dirty hack to use system fast_float
  ln -sf "${TRACK_ROOT}/usr/include/fast_float" \
         "${SOURCE_DIRECTORY}/3rdparty/fast_float/include"
fi
