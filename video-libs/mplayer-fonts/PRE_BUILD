mk_source_dir  $SOURCE_DIRECTORY    &&
cd             $SOURCE_DIRECTORY    &&
if [ "$ISO88591" == "y" ] ;then
  unpack_file
fi &&
if [ "$ISO88592" == "y" ] ;then
  unpack_file 2
fi &&
if [ "$ISO88597" == "y" ] ;then
  unpack_file 3
fi &&
if [ "$CP1250" == "y" ] ;then
  unpack_file 4
fi

