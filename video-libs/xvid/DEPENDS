if test "${SMGL_COMPAT_ARCHS[1]}" = "x86_64"; then
  optional_depends  yasm  ""  "--disable-assembly"  "for faster code"
elif test "${SMGL_COMPAT_ARCHS[1]}" = "ia32"; then
  optional_depends  X86-ASSEMBLER  ""  "--disable-assembly"  "for faster code"
fi
