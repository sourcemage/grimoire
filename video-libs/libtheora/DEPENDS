depends  automake  &&
depends  libogg    &&

optional_depends  sdl                \
                  ""                 \
                  ""                 \
                  "for SDL support"

if [ "$THEORA_SVN" = "y" ]; then
  depends  subversion  &&
  depends  automake    &&
  depends  libtool
fi
