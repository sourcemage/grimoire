default_pre_build      &&
cd  $SOURCE_DIRECTORY  &&
# fix compile without X11-LIBS, bug #10089
patch  configure  $SCRIPT_DIRECTORY/disable_x.diff  &&
# fix playback of files encoded with wavpack -h
patch  -p0  <  $SCRIPT_DIRECTORY/gst-plugins-wavpack.patch
