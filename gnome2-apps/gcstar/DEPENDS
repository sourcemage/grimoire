depends perl-gtk2  &&
depends lwp  &&
depends xml-simple &&

if [ $TELLICO_IMPORT = 'y' ]; then
  depends  archive-zip 
fi  &&

if [ $TARGZ_SUPPORT = 'y' ]; then
  depends archive-tar &&
  depends  compress-zlib
fi
