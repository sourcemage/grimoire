mk_source_dir ${SOURCE_DIRECTORY}                                &&
unpack_file   ''         &&
mkdir -p      ${SOURCE_DIRECTORY}/src                            &&
cp            ${SOURCE_CACHE}/${SOURCE2} ${SOURCE_DIRECTORY}/src &&
cp            ${SOURCE_CACHE}/${SOURCE3} ${SOURCE_DIRECTORY}/src &&
cp            ${SOURCE_CACHE}/${SOURCE4} ${SOURCE_DIRECTORY}/src &&
cp            ${SOURCE_CACHE}/${SOURCE5} ${SOURCE_DIRECTORY}/src &&
cp            ${SOURCE_CACHE}/${SOURCE6} ${SOURCE_DIRECTORY}/src &&
cp            ${SOURCE_CACHE}/${SOURCE7} ${SOURCE_DIRECTORY}/src &&
cp            ${SOURCE_CACHE}/${SOURCE8} ${SOURCE_DIRECTORY}/src 

