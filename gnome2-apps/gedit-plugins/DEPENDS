depends gedit         &&
depends rarian        &&

optional_depends python                    \
                 "--enable-python"         \
                 "--disable-python"        \
                 "Enable Python support?"
