depends gedit2        &&
depends scrollkeeper  &&

optional_depends python                    \
                 "--enable-python"         \
                 "--disable-python"        \
                 "Enable Python support?"
