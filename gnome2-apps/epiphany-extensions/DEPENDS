depends epiphany  &&
depends python    &&

optional_depends opensp                                      \
                 "--enable-opensp=yes"                       \
                 "--enable-opensp=no"                        \
                 "Attempt to use OpenSP for SGML validation" &&

optional_depends rarian                                      \
                 "--enable-scrollkeeper"                     \
                 "--disable-scrollkeeper"                    \
                 "to register with scrollkeeper database"
