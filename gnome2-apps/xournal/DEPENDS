depends gtk+2 &&
depends libgnomecanvas &&
depends poppler &&
optional_depends ghostscript \
                 "--with-ghostscript" \
                 "--without-ghostscript" \
                 "used to import PS/PDF files as bitmap backgrounds"
