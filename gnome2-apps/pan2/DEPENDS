if [ "$PAN2_CVS" == "y" ]; then
        depends CVS   &&
        depends automake
fi &&
depends libxml2 &&
depends gtk+2   &&
depends gnet    &&
depends pcre    &&
depends g++     &&

optional_depends gtkspell "--enable-gtkspell" "" "Add gtkspell support"

