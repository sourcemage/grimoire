local PATH=".:$PATH"                   &&
OPTS="$OPTS --enable-alsa"             &&

if [ "$SINGLE" == "y" ]; then
   OPTS="$OPTS --enable-single-fftw3"
fi &&

default_build                          
