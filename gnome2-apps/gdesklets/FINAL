$INSTALL_ROOT/usr/bin/update-mime-database $INSTALL_ROOT/usr/share/mime  &&
$INSTALL_ROOT/usr/bin/update-desktop-database $INSTALL_ROOT/usr/share/applications  &&
echo "You will have to visit http://gdesklets.gnomedesktop.org and download some of the Displays, since it only supports displays instal
l on a per-user basis at the moment"
