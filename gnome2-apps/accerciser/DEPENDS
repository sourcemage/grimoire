depends intltool &&
depends gnome-doc-utils &&
depends python &&
depends docbook-dtd &&

optional_depends rarian \
                 "--enable-scrollkeeper" \
                 "--disable-scrollkeeper" \
                 "to register with rarian"
