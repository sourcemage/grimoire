if [ "$REL" == "cvs" ]; then
        depends subversion   &&
        depends automake
fi &&
depends gmime   &&
depends gtk+2   &&
depends pcre    &&
depends g++     &&

optional_depends gtkspell                    \
                    "--with-gtkspell"      \
                    ""                       \
                    "Add gtkspell support"
