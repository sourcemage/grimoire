depends bullet &&
depends fribidi &&
depends libxp &&
depends subversion &&
depends valgrind &&

optional_depends check "" "" "Use check?"  &&

optional_depends doxygen                   \
                 "--enable-doc"            \
                 "--disable-doc"           \
                 "Build documentation?"  
