config_query  E_DBUS_CVS  "Build SCM (subversion) version?"  y  &&

if [ "$E_DBUS_CVS" == "y" ]; then
   config_query  E_DBUS_CVS_AUTOUPDATE                       \
                 "Automatically update $SPELL on system-update?"  n
fi
