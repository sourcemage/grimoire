depends util-linux &&
depends ecore &&
depends eet &&
depends DEVICE-MANAGER &&

optional_depends doxygen \
                 '--enable-doc' \
                 '--disable-doc' \
                 'to build documentation'

