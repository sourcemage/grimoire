depends util-linux &&
depends ecore &&
depends eet &&
depends UDEV &&

optional_depends doxygen \
                 '--enable-doc' \
                 '--disable-doc' \
                 'to build documentation'

