depends subversion &&
depends efl   &&
depends ecore &&

optional_depends check "" "" "Use check?"  &&

optional_depends doxygen                   \
                 "--enable-doc"            \
                 "--disable-doc"           \
                 "Build documentation?"  
