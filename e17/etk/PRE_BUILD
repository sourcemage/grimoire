default_pre_build             &&
cd  $SOURCE_DIRECTORY         &&

if grep proto CVS/Repository > /dev/null; then
message "${PROBLEM_COLOR}SOURCE_URL of etk was changed, please remove old /var/spool/sorcery/etk-cvs.tar.bz2${DEFAULT_COLOR}"  &&
return 1
fi  &&

sedit  "s/; aclocal/; aclocal-1.9/" autogen.sh &&
sedit  "s/; automake/; automake-1.9/" autogen.sh &&
NOCONFIGURE=ON  ./autogen.sh
