. "${GRIMOIRE}/MESON_CONFIGURE" &&

config_query_option GTK_LAYER_SHELL_OPTS "Enable tests?" n "tests=true" "tests=false" &&
config_query_option GTK_LAYER_SHELL_OPTS "Enable examples?" n "examples=true" "examples=false"
