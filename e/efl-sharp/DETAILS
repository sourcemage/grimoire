           SPELL=efl-sharp
if  [  "$EFL_SHARP_SVN_AUTOUPDATE"  ==  "y"  ];  then
         VERSION=$(date +%Y%m%d)
else
         VERSION=svn
fi
          SOURCE=$SPELL-svn.tar.bz2
SOURCE_DIRECTORY=$BUILD_DIRECTORY/$SPELL-$VERSION
   SOURCE_URL[0]=svn://svn.myrealbox.com/source/trunk/efl-sharp/libs:efl-sharp
  FORCE_DOWNLOAD=on
   SOURCE_IGNORE=volatile
      LICENSE[0]=GPL
       BUILD_API=2
        WEB_SITE=http://efl-sharp.sourceforge.net/
         ENTERED=20060202
           SHORT='Enlightenment Foundation Libraries C# bindings'
cat << EOF
The EFL are libraries that were created to support the creation of the
Enlightenment desktop environment. The EFL-Sharp libraries will allow a
developer to create applications using Enlightenment's technologies within the
Mono platform using the C# language.
EOF
