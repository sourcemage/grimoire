depends glib &&
depends libx11 &&
depends libxext &&

optional_depends libsm \
                 "--enable-shm" \
                 "--disable-shm" \
                 "for shared memory support" &&

optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
