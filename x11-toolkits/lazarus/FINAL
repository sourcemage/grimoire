#local CFG                          &&
#CFG=$INSTALL_ROOT/etc/fpc.cfg      &&
#sed -i "/lazarus/D" $CFG           &&
#cat >> $CFG << EOF
## Add lazarus libs
#-Fu/$INSTDIR/lcl/units
#-Fu/$INSTDIR/lcl/units/$LAZ_GUI
#-Fu/$INSTDIR/components/units
#-Fu/$INSTDIR/components/units/$LAZ_GUI
#EOF
cat << EOF
you will need to have installed the source files for fpc

you might want to force the conversion of file names to lowercase
in Environment > Environment options > files
EOF
