make install INSTALL_PREFIX=${INSTALL_ROOT}/usr/share/lazarus
#  INSTDIR=${INSTALL_ROOT}/usr/share/lazarus                  &&

## install sources, etc as needed later
#  mkdir -p $INSTDIR                                          &&
#  chown pascal:pascal $INSTDIR                               &&
#  cp -r ./* $INSTDIR                                         &&
#  ln -sf $INSTDIR/lazarus  $INSTDIR/startlazarus ${INSTALL_ROOT}/usr/bin
  
