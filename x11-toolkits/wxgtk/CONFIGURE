
# may need to ask question about this
# note: don't add `xrc' here, it's just a resource directory
#WX_CONTRIB='animate gizmos ogl stc' &&
#persistent_add WX_CONTRIB
source $GRIMOIRE/config_query_multi.function  &&
if [[ "$GTK_DEV" == "y" ]];then
  config_query_multi WX_CONTRIB "which add-ons to build?" animate applet canvas foldbar \
                                 fl gizmos mmedia net ogl stc

else
  config_query_multi WX_CONTRIB "which add-ons to build?" animate applet foldbar \
                                 fl gizmos mmedia net ogl stc
fi
