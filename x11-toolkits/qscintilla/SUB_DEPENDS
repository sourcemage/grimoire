case $THIS_SUB_DEPENDS in
QT4) echo "QT4 support requested, forcing QT4 dependency." &&
     depends qt4
     ;;
QT3) echo "QT3 support requested, forcing QT4 dependency." &&
     depends qt-x11
     ;;
*) echo unknown sub-depends!!! ; return 1 ;;
esac
