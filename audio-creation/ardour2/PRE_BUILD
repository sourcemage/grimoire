default_pre_build &&
cd "$SOURCE_DIRECTORY" &&

patch -p0 < "$SPELL_DIRECTORY/mime.patch" &&
patch -p1 < "$SPELL_DIRECTORY/gcc46.patch" &&

sed -i "s:soundtouch-1.0:soundtouch-1.4:" SConstruct &&
# Brute force lib64 killing.
sed -i "s:lib64:lib:" SConstruct
