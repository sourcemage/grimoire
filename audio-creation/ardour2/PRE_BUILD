default_pre_build &&
cd "$SOURCE_DIRECTORY" &&

patch -p0 < "$SPELL_DIRECTORY/mime.patch" &&
patch -p0 < "$SPELL_DIRECTORY/libsndfile-ext.patch" &&

sed -i "s:soundtouch-1.0:soundtouch-1.4:" SConstructt &&
# Brute force lib64 killing.
sed -i "s:lib64:lib:" SConstruct
