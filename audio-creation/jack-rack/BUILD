#
# speed up build, install desktop files with $INSTALL_ROOT, make sure
# deprecated LADCCA isn't used
#
OPTS="--disable-dependency-tracking --disable-desktop-inst  \
      --disable-ladcca $OPTS"                               \
default_build
