. $GRIMOIRE/KDE5_FUNCTIONS
default_build() {
 OPTS+=" -D${SPELL}_SOURCE_DIR=$SOURCE_DIRECTORY"
 kde5_build
}


default_pre_build() {
mkdir -p $SOURCE_DIRECTORY &&
cd  $SOURCE_DIRECTORY &&
unpack_file           &&
mv $SPELLX-$VERSION/$SPELL/* . &&
rm -r $SPELLX-$VERSION
}


