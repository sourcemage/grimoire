#default_pre_build() {
mkdir -p $SOURCE_DIRECTORY &&
cd  $SOURCE_DIRECTORY &&
unpack_file           &&
mv $SPELLX-$VERSION/$SPELLZ/* . &&
rm -r $SPELLX-$VERSION &&
#default_pre_build    &&
cd $SOURCE_DIRECTORY &&
sed -i '/update_xdg_mimetypes/D' src/CMakeLists.txt
