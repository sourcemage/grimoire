source $GRIMOIRE/MESON_FUNCTIONS

# some default settings, which can be overridden if needed
#PREFIX=${PREFIX:-/opt/test}
PREFIX=${PREFIX:-/usr}
LIBDIR=${LIBDIR:-lib}
INCLUDEDIR=${INCLUDEDIR:-include}
DATADIR=${DATADIR:-share}
MANDIR=${MANDIR:-$DATADIR/man}

export PKG_CONFIG_PATH=/opt/qt6/lib/pkgconfig
