depends cmake &&
depends libsodium &&
depends libvpx &&
depends opus &&

# For DHT bootstrap daemon
depends libconfig &&

# For nTox
optional_depends ncurses -DBUILD_NTOX={1,0} \
                 "for building the nTox client"

# For the Spec. tests
#depends msgpack-c &&

# For the A/V tests
#depends libsndfile &&
#depends OPENCV &&
#depends portaudio19
