depends -sub CXX gcc     &&
depends gnutls  &&
depends openssl &&
depends libidn  &&
optional_depends zlib \
                 "--with-zlib" \
                 "--without-zlib" \
                 "for Stream Compression"
