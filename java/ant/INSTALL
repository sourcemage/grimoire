mkdir -p /opt/ant                         &&
cp -R antbuild/* /opt/ant                 &&
mkdir -p ${INSTALL_ROOT}/etc/profile.d/   &&
install -m 0644 $SCRIPT_DIRECTORY/ant.sh  \
         ${INSTALL_ROOT}/etc/profile.d/
