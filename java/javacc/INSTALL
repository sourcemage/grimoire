cd bin/ &&
for bin in javacc jjdoc jjtree
do
	sedit 's#`dirname $0`/lib#/usr/lib/javacc#g' $bin &&
	cp -a $bin ${INSTALL_ROOT}/usr/bin
done &&
cd lib/ &&
mkdir -v ${INSTALL_ROOT}/usr/lib/javacc &&
for lib in javacc.jar
do
	cp -a $lib ${INSTALL_ROOT}/usr/lib/javacc
done
