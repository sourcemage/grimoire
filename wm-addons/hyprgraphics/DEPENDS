. "${GRIMOIRE}/CMAKE_DEPENDS" &&
if [[ "${HYPRGRAPHICS_BRANCH}" == "scm" ]]; then
  depends git
fi &&
depends cairo &&
depends hyprutils &&
depends libwebp &&
depends JPEG &&
depends libjxl &&
depends pixman
