install -m 0755 lal ${INSTALL_ROOT}/usr/bin/ &&
if [[ "$GATHER_DOCS" = 'on' ]]; then
  mkdir -p ${INSTALL_ROOT}/usr/share/doc/$SPEL &&
  install -m 0644 README COPYING ${INSTALL_ROOT}/usr/share/doc/$SPEL/
fi
