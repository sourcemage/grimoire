. "${GRIMOIRE}/CMAKE_DEPENDS" &&

if [[ "${HYPRUTILS_BRANCH}" == "scm" ]]; then
  depends git
fi &&

depends pixman &&
depends PKG-CONFIG &&

optional_depends googletest "-DBUILD_TESTING=ON" "-DBUILD_TESTING=OFF" "for tests"
