. "${GRIMOIRE}/CMAKE_DEPENDS" &&

if [[ "${HYPRIDLE_BRANCH}" == "scm" ]]; then
  depends git
fi &&

depends hyprlang &&
depends hyprutils &&
depends sdbus-cpp &&
depends wayland &&
depends wayland-protocols
