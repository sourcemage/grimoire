# doesn't build with gtk+2
depends gtk+3 --with-gtk3 &&
# TODO Add appindicator support, needs ayatana-appindicator
optional_depends GETTEXT \
                 '--enable-nls' \
                 '--disable-nls' \
                 'for Native Language Support' &&
suggest_depends xdotool '' '' 'for automatic paste support'
