           SPELL=gnustep-terminal
if [ "$TERMAPP_CVS_AUTOUPDATE" == "y" ];
then
         VERSION=$(date +%Y%m%d)
else
	 VERSION=cvs
fi
          SOURCE=Terminal-cvs.tar.bz2
SOURCE_DIRECTORY=$BUILD_DIRECTORY/Terminal-cvs
   SOURCE_URL[0]=cvs://:pserver:anoncvs@cvs.savannah.gnu.org:/cvsroot/backbone:System
        WEB_SITE=http://www.nongnu.org/backbone
   SOURCE_IGNORE=volatile
      LICENSE[0]=GPL
  FORCE_DOWNLOAD=1
         ENTERED=20050908
         UPDATED=20050908
       BUILD_API=2
        KEYWORDS="terminal gnustep"
           SHORT="GNUstep terminal app from backbone project."

cat << EOF
GNUstep terminal app from backbone project.
EOF

