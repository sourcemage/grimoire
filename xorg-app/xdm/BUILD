OPTS="$XDM_OPTS $OPTS"
OPTS="${OPTS} ${XDM_XPRINT}"          &&
OPTS="${OPTS} ${XDM_UNIX_TRANSPORT}"  &&
OPTS="${OPTS} ${XDM_TCP_TRANSPORT}"   &&
OPTS="${OPTS} ${XDM_IPV6}"            &&
OPTS="${OPTS} ${XDM_SECURE_RPC}"      &&
OPTS="${OPTS} ${XDM_XPM_LOGOS}"       &&
OPTS="${OPTS} ${XDM_PAM}"             &&

default_build
