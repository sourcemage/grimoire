depends util-macros &&
depends libfs &&
depends libxfont &&
depends fontsproto &&
depends xtrans &&

optional_depends  SYSTEM-LOGGER        \
                  '--enable-syslog'    \
                  '--disable-syslog'   \
                  'for system logger'
