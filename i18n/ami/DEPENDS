depends gdk-pixbuf &&
depends gtk+ &&
depends glib &&
if [[ $(get_spell_provider gtk+ X11-LIBS) == "xorg-server" ]]; then
  depends libx11 &&
  depends libxext &&
  depends libxau &&
  depends libxdmcp
fi
