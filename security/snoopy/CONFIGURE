config_query SNOOPY_ENABLE "Enable snoopy logger in /etc/ld.so.preload?" n &&

config_query_option SNOOPY_OPTS "Enable logging of Current Working Directory?" y \
                                "--enable-cwd-logging" \
                                "--disable-cwd-logging"
