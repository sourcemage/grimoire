depends git &&
depends libusb &&
depends libfprint &&
depends dbus &&
depends gtk-doc &&
optional_depends linux-pam '--enable-pam'            \
                           '--disable-pam'           \
                           'build the pam module (broken)'
