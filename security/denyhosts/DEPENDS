message  "${SPELL_COLOR}$SPELL${DEFAULT_COLOR}${MESSAGE_COLOR} depends on"  \
         "SSH being built with ${DEFAULT_COLOR}${SPELL_COLOR}tcp_wrappers"  &&

depends  SSH           &&
depends  tcp_wrappers  &&
depends  python
