depends shadow &&

optional_depends linux-pam \
                 "--with-pam" \
                 "--without-pam" \
                 "for PAM support" &&

suggest_depends SYSTEM-LOGGER "" "" "for logging to syslog"
