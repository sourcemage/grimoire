config_query_option OPENDOAS_OPTS "Enable timestamp (persist feature) support?" n \
                                  "--with-timestamp" \
                                  "--without-timestamp" &&

config_query SUDO_SYMLINK "Would you like to have /usr/bin/sudo as a symlink to /usr/bin/doas?" n
