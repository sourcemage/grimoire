if [[ -n $OPTS ]]; then
local i  &&
local OPTS1  &&
for i in $OPTS ; do
   i=$i-install  &&
   OPTS1="${OPTS1} $i"
done
fi
make PREFIX="${INSTALL_ROOT}/usr" \
     headers-install \
     libs-install \
     nss-install \
     rklogd-install \
     tools-install \
     $OPTS1
