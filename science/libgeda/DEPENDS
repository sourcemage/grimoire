depends guile &&
depends gtk+2 &&
depends glib2 &&
depends gdk-pixbuf &&

optional_depends "libpng"           \
                 ""                 \
                 "--without-libpng" \
                 "for png support"
