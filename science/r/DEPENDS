depends readline &&
depends ncurses  &&
# Sergeys change: depends fortran  &&
depends g77      &&
depends g++      &&

# also looks for but not sure if it needs
# less, perl, TeX, laTeX, unzip, zip, gzip,
# mozilla, acroread, xpdf, libtool.

optional_depends zlib                    \
                 "--with-zlib"           \
                 "--without-zlib"        \
                 "Use system zlib"       &&
optional_depends bzip2                   \
                 "--with-bzlib"          \
                 "--without-bzlib"       \
                 "Use system bzip2"      &&
optional_depends libpng                  \
                 "--with-libpng"         \
                 "--without-libpng"      \
                 "Enable png support"    &&
optional_depends jpeg                    \
                 "--with-jpeglib"        \
                 "--without-jpeglib"     \
                 "Enable jpeg support"   &&
optional_depends pcre                    \
                 "--with-pcre"           \
                 "--without-pcre"        \
                 "Enable pcre support"   &&
optional_depends tk                      \
                 "--with-tcltk"          \
                 "--without-tcltk"       \
                 "Enable TCL/TK support" 
