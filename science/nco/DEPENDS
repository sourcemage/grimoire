# It can also use UDUnits and other fun ... need to add spells later.
depends netcdf &&
optional_depends gsl \
                 "--enable-gsl" \
                 "--disable-gsl" \
                 "for statistics" 
