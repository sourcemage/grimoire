cd $BUILD_DIRECTORY &&
unpack $SOURCE1 $MD5[1] &&

if grep -q 'triangle="y"'  ${SPELL_CONFIG}
then
  mkdir -p $SOURCE_DIRECTORY/tmp &&
  cd $SOURCE_DIRECTORY/tmp &&
  unpack $SOURCE2 $MD5[2]
  cp triangle.c ../Triangle &&
  cp triangle.h ../Triangle
fi

cd $SOURCE_DIRECTORY 
