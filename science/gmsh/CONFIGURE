if ! grep -q CONFIGURED ${SPELL_CONFIG}
then
  if    query  "Do you want to use triangle for Delaunay triangulations?" y
  then  echo   'triangle="y"' >> ${SPELL_CONFIG}
  fi

  echo  "CONFIGURED='y'"  >>  ${SPELL_CONFIG}
fi
