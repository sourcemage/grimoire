# couldn't successfully build server so going to leave the logic, but force the decision to be client
#config_query_list BOINC_PARTS "Which part of boinc would you like to build?"  \
#                              both client server                              &&

config_query  BOINC_SETUID  "Install boinc_client-svn setuid/setgid so it always runs as the boinc user?"  y
