config_query_list BOINC_PARTS "Which part of boinc would you like to build?"  \
                              both client server                              &&

config_query  BOINC_SETUID  "Install boinc_client-cvs setuid/setgid so it always runs as the boinc user?"  y
