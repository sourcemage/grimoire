default_pre_build       &&
cd "$SOURCE_DIRECTORY"  &&

apply_patch_dir patches &&

# Fix build against Boost 1.89+
sedit 's/ -lboost_system / /' features/boost.prf &&

# Fix .pc name for lib3mf
sedit '/^ *find_package/s/Lib3MF/lib3mf/' CMakeLists.txt
