default_pre_build &&
cd $SOURCE_DIRECTORY &&
unpack_file 2 &&
cd ossim_plugins-scm &&
make make_options &&
echo "OSSIM_HOME=$SOURCE_DIRECTORY" >> make.opt &&
sed -i 's,libtar.h,,' ../configure #not used, but would require libtar as dep
