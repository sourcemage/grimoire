. "$GRIMOIRE"/VDEPENDS &&
vdepends << ! &&
	alsa-lib
	gtk+3 >= 3.4
	gtkdatabox >= 1.0.0
	fftw
	perl
!
optional_depends comedilib \
                 "--with-comedi" \
                 "--without-comedi" \
                 "for COMEDI devices support"
