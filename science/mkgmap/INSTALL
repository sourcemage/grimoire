mkdir -p ${INSTALL_ROOT}/usr/share/mkgmap                                           &&
install -m644 ${SOURCE_DIRECTORY}/dist/mkgmap.jar ${INSTALL_ROOT}/usr/share/mkgmap/ &&
install -m755 ${SCRIPT_DIRECTORY}/mkgmap ${INSTALL_ROOT}/usr/bin/
