mk_source_dir $SOURCE_DIRECTORY &&
unpack_file                     &&
cd $SOURCE_DIRECTORY            &&
echo "PATCHING"                 &&
patch -p1 < $SPELL_DIRECTORY/qalculate-gtk-0.9.6-cln-config.patch
