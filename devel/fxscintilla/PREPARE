config_query FXSCINTILLA_CVS "Use CVS version?" n     &&

if [[ "$FXSCINTILLA_CVS" == "y" ]]
then
  config_query  FXSCINTILLA_CVS_AUTOUPDATE            \
                "Automatically update every day?"  n
fi
