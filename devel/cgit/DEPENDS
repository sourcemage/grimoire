depends xz-utils &&
depends SSL &&

optional_depends lua "" "NO_LUA=1" "for Lua scripting support" &&
optional_depends asciidoc "" "" "to generate documentation" &&

runtime_depends gzip &&
runtime_depends bzip2 &&

suggest_depends zip "" "" "for zip compressed snapshots support" &&
suggest_depends lzip "" "" "for lzip compressed snapshots support" &&
suggest_depends zstd "" "" "for zstd compressed snapshots support"
