default_pre_build     &&
cd $SOURCE_DIRECTORY  &&
verify_file 2         &&
# apply Debian patch
zcat $SOURCE_CACHE/$SOURCE2 | patch -p1  &&
# use Sourcemage flags
sed -i "s/-O2 -g -Wall/$CFLAGS/"  bootstrap.sh &&
chmod u+x *.sh
