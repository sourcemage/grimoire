default_pre_build     &&
cd $SOURCE_DIRECTORY  &&
verify_file 2         &&
# apply Debian patch
zcat $SOURCE_CACHE/$SOURCE2 | patch -p1  &&
# apply ggc-4.1 patches
patch -p1 < $SPELL_DIRECTORY/gcc.diff    &&
# use Sourcemage flags
sed -i "s/-O2 -g -Wall/$CFLAGS/"  bootstrap &&
chmod u+x bootstrap
