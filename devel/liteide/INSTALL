install -Dm444 "liteide.desktop" "$INSTALL_ROOT"/usr/share/applications/ &&
cd liteide &&
cp -vrf bin lib share "$INSTALL_ROOT"/usr &&
cd ../../liteidex &&
cp -vrf deploy/* os_deploy/* "$INSTALL_ROOT"/usr/lib/liteide &&
ln -sf ../liteide/welcome/images/liteide400.png \
    "$INSTALL_ROOT"/usr/share/pixmaps/liteide.png
