  PREFIX=${INSTALL_ROOT}/usr   &&
  cd src/unix      &&
  ./configure --prefix=${PREFIX} --mandir=${PREFIX}/share/man \
     --infodir=${PREFIX}/share/info  \
     $OPTS &&
  cd ..            &&
  make clean       &&
  make             
