depends -sub ADA gcc    &&
depends xmlada &&
optional_depends openssl \
                 "--with-openssl" \
                 "--without-openssl" \
                 "build SSL support and SSL dependent features"
