depends gdk-pixbuf2 &&
depends glib2 &&
depends gmp &&
depends mpfr &&
depends gtk+2 &&
depends pango &&
depends ocaml &&
optional_depends sqlite3-ocaml \
                 "--enable-bench" \
                 "--disable-bench" \
                 "support for batch processing"
