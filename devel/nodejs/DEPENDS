depends -sub CXX gcc &&
depends python &&
depends zlib '--shared-zlib' &&

optional_depends openssl \
                 "--shared-openssl" \
                 "" \
                 "to use system OpenSSL library instead of bundled"
