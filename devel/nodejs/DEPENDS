depends -sub CXX gcc &&
depends python &&
depends zlib '--shared-zlib' &&

optional_depends SSL \
                 "--shared-openssl" \
                 "" \
                 "to use system SSL library instead of bundled"
