default_pre_build        &&
cd  "$SOURCE_DIRECTORY"  &&

patch -p1 < "$SPELL_DIRECTORY/glibc.patch" &&

if [[ $VALGRIND_BRANCH == "scm" ]]; then
  NOCONFIGURE=Y  ./autogen.sh
else
  autoreconf -fi
fi
