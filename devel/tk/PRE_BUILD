mk_source_dir  $SOURCE_DIRECTORY                    &&
mk_source_dir  $SOURCE2_DIRECTORY                   &&
unpack_file                                         &&
unpack_file 2                                       &&
cd ${SOURCE_DIRECTORY}                              &&
patch -p0 < ${SCRIPT_DIRECTORY}/tcl-configure.diff  &&
cd ${SOURCE2_DIRECTORY}                             &&
patch -p0 < ${SCRIPT_DIRECTORY}/tk-configure.diff 
