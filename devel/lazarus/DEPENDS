depends fpc             &&
depends fpcsrc          &&
case $LAZ_GUI in
  gtk) depends gtk+     &&
      depends gdk-pixbuf
	;;
  gtk2) depends gtk+2
	;;
  qt4) depends qt4      &&
      depends g++       &&
      depends qt4pas
	;;
esac
