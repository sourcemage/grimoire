default_pre_build &&
# This breaks on gcc 9.1.0 and glibc 2.29, no musl
# I tried downgrading -fstack-protector-strong to just -fstack-protector with sed, but that still segfaulted.
# gcc with non-musl is built with '--enable-default-ssp', but with musl is built with '--disable-default-ssp'
# see `git log PRE_BUILD` for error
#cd "$SOURCE_DIRECTORY" &&
#sedit 's@gccversion" in@& [1-3][!.]*|[4-9]*|\\@' Configure  &&
