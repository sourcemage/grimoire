if [ -z $(installed_version guile) ]
then
    return 0
fi                                                   &&

local VE=$(installed_version guile)
if [[ ${VE:0:3} = "1.6" && ${VERSION:0:3} == "1.8" ]]
then
    RECAST_UP="guile"
fi
