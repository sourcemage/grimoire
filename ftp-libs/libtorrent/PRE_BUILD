default_pre_build                          &&
cd ${SOURCE_DIRECTORY}                     &&
# fix problems on amd64 with libtool versions (bug #13943)
patch -p1 < $SCRIPT_DIRECTORY/gcc43.patch  &&
./autogen.sh
