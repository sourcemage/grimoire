cd $SOURCE_DIRECTORY.static &&
make prefix=$INSTALL_ROOT/usr install &&
cd $SOURCE_DIRECTORY &&
make prefix=$INSTALL_ROOT/usr install &&
cd $INSTALL_ROOT/usr/lib/pkgconfig &&
ln -s $TRACK_ROOT/lib/pkgconfig/zlib.pc
