optional_depends JPEG \
                 "--with-jpeg" \
                 "--without-jpeg" \
                 "for jpeg support" &&

optional_depends zlib \
                 "--with-zlib" \
                 "--without-zlib" \
                 "for deflate support"
