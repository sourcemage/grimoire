depends  zlib  '--with-installed-zlib'  &&
depends  X11-SERVER                     &&

.  $GRIMOIRE/FUNCTIONS  &&

if check_if_xorg_modular_server;  then
  depends  libice   &&
  depends  libsm    &&
  depends  libxext  &&
  depends  libxtst
fi &&

if [[ $VNC_SERVER == y ]]; then
  depends autoconf &&
  depends automake &&
  depends libtool
fi
