depends  zlib  '--with-installed-zlib'  &&
depends  libice     &&
depends  libsm      &&
depends  libx11     &&
depends  libxau     &&
depends  libxcb     &&
depends  libxdmcp   &&
depends  libxext    &&
depends  libxi      &&
depends  libxtst    &&

if [[ $VNC_SERVER == y ]]; then
  depends autoconf &&
  depends automake &&
  depends libtool
fi
