depends  zlib  '--with-installed-zlib'  &&
depends  xorg-libs                       &&

.  $GRIMOIRE/FUNCTIONS  &&

if  check_if_xorg_modular_libs;  then
  depends  libice   &&
  depends  libsm    &&
  depends  libxext  &&
  depends  libxtst
fi
