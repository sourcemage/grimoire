depends  zlib  '--with-installed-zlib'  &&
depends  xorg-libs  &&
depends  libice     &&
depends  libsm      &&
depends  libxext    &&
depends  libxtst    &&

if [[ $VNC_SERVER == y ]]; then
  depends autoconf &&
  depends automake &&
  depends libtool
fi
