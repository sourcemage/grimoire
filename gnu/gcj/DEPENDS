. $GRIMOIRE/libcompat                                               &&
depends  gcc                                                        &&
if [ "$GCJ_AWT" = "y" ]; then
	depends gtk+2 '--enable-java-awt=gtk'                       &&
	depends libart_lgpl
fi                                                                  &&
if  [[  $VERSION != $(installed_version gcc) ]]; then
  force_depends  gcc
fi
