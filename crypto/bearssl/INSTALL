# TODO: fix the soname after v1.0
install -v -D -m555 build/libbearssl.so \
    "$INSTALL_ROOT"/usr/lib/libbearssl.so.$VERSION &&
ln -sf libbearssl.so.${VERSION} "$INSTALL_ROOT"/usr/lib/libbearssl.so &&
install -v -D -m444 -t "$INSTALL_ROOT"/usr/include inc/*.h
