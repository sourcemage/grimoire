optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for native language support" &&

suggest_depends emacs \
                "--enable-emacs" \
                "--disable-emacs" \
                "for emacs support"
