default_pre_build &&
cd "$SOURCE_DIRECTORY" &&
apply_patch_dir patches &&
# preserve old libs
mkdir old3 &&
for SSL in ssl crypto;do
  if [[ -f "$INSTALL_ROOT/usr/lib/lib${SSL}.so.3" ]];then
    cp "$INSTALL_ROOT/usr/lib/lib${SSL}.so.3" old3
  fi
done

