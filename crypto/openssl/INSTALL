make MANDIR="$INSTALL_ROOT/usr/share/man" install &&
# preserve old libs
for SSL in ssl crypto;do
  if [[ -f "old3/lib${SSL}.so.3" ]];then
    cp "old3/lib${SSL}.so.3" "$INSTALL_ROOT/usr/lib/" 
  fi
done

