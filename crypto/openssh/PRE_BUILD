default_pre_build &&

cd "$SOURCE_DIRECTORY" &&
patch -Np1 < "$SCRIPT_DIRECTORY/scp-name-validator.patch" &&

if [ "${OPENSSH_LPK}" == "y" ]; then
  cd "${SOURCE_DIRECTORY}" &&
  zcat $SPELL_DIRECTORY/openssh-lpk-6.5p1-0.3.14.patch.gz | patch -p1 &&
  autoconf
fi
