source  "$SECTION_DIRECTORY/KDE_DEPENDS"  &&

depends  kdebase     &&
depends  lcms        &&
depends  libgphoto2  &&
depends  libkipi     &&
depends  imlib2      &&
depends  sqlite      &&

if [[ "$DG_VER" == "y" ]]; then
  depends exiv2
else
  depends libkexif
fi
