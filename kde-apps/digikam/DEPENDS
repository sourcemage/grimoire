source  "$SECTION_DIRECTORY/KDE_DEPENDS"  &&

depends  kdebase     &&
depends  lcms        &&
depends  libgphoto2  &&
depends  libkipi     &&
depends  sqlite      &&
depends  libkexiv2   &&
if [[ "$DG_VER" == "y" ]];then
  depends  libkdcraw
else
  depends  imlib2
fi &&
depends  jasper
