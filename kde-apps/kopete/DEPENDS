depends  libxslt  &&
depends  libidn   &&

source  "$SECTION_DIRECTORY/KDE_DEPENDS"  &&

optional_depends  qca-tls            \
                  ""                 \
                  ""                 \
                  "for SSL support"  &&

optional_depends xmms                                          \
                 ""                                            \
                 ""                                            \
                 "for Kopete's Now Listening plugin for xmms"  &&

source  "$GRIMOIRE/FUNCTIONS"  &&
if check_if_xorg_modular_libs qt-x11
then
  depends libx11 &&
  depends libxext &&
  depends libxrender
fi
