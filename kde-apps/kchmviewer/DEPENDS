depends jpeg &&
depends libpng &&
depends zlib &&
depends chmlib &&

if list_find "$KCHMVIEW_OPTS" "--with-kde" ; then

   if [[ $QT3 == y ]]; then

      depends kdebase &&

      .  "$SECTION_DIRECTORY/KDE_DEPENDS"

   fi

else

   if [[ $QT3 == y ]]; then

      depends qt-x11

   else

      depends qt4

   fi

fi
