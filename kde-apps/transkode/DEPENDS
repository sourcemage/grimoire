depends taglib &&

.  "$SECTION_DIRECTORY/KDE_DEPENDS" &&

suggest_depends "faac" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "ffmpeg" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "flac" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "gstreamer" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "lame" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "mplayer" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "muse" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "libvorbis" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "vorbis-tools" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "shorten" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "speex" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "streamripper" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "twolame" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "wavpack" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "xine-lib" "" "" "do you want to use it as encoding/decoding backend for $SPELL?" &&
suggest_depends "ttaenc" "" "" "do you want to use it as encoding/decoding backend for $SPELL?"
