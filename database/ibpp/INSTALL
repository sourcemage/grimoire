  cd $SOURCE_DIRECTORY               &&
  DEST=$TRACK_ROOT/usr/share/ibpp    &&
  mkdir -p $DEST                     &&
  cp -r *.txt core $DEST             &&
  cp build/*.{so,a} $TRACK_ROOT/usr/lib/  &&
  cp core/ibpp.h $TRACK_ROOT/usr/include
