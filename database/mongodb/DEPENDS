. "$GRIMOIRE/FUNCTIONS" &&

depends -sub CXX gcc &&
depends scons &&
depends readline &&
depends spidermonkey '--usesm' &&

if spell_ok spidermonkey && is_version_less $(installed_version spidermonkey) 1.8.5; then
  force_depends spidermonkey
fi &&

depends -sub "THREAD FILESYSTEM PROGRAM_OPTIONS" boost
