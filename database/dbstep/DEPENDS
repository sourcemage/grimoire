depends g++ &&
optional_depends "sqlite" "--with-sqlite" "--disable-sqlite" \
			"sqlite database support"	&&
optional_depends "mysql" "--with-mysql" "--disable-mysql" \
			"database support"		&&
optional_depends "postgresql" "--with-pgsql" "--disable-pgsql" \
			"database support"		&&
optional_depends "firebird" "--with-fbird=/usr/firebird" "--disable-fbird" \
			"database support"		&&
optional_depends "ODBC-MGR" "--with-odbc" "--disable-odbc" \
			"database support"

