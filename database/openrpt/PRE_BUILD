default_pre_build          &&
cd $SOURCE_DIRECTORY 	   &&
# allow access to any dbms
bzcat $SPELL_DIRECTORY/multidb.diff.bz2 | patch -p0  &&
# use dynamic libraries
sed -i -e "s/staticlib//" common/common.pro OpenRPT/renderer/renderer.pro \
     OpenRPT/wrtembed/wrtembed.pro
