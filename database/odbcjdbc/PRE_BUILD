if [[ "$WHICH" == "stable" ]]; then
  mk_source_dir $SOURCE_DIRECTORY	                         &&
  cd $SOURCE_DIRECTORY 			                         &&
  unpack $SOURCE ${MD5[0]}
else
 default_pre_build
fi
