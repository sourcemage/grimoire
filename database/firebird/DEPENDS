depends g++             &&
depends libtool		&&
depends openssl		&&
depends libmcrypt	&&
depends ncurses		&&
if [ $FB_VERX != old ]; then
#else
  depends icu
fi                      &&
optional_depends libedit "--with-editline" "--without-editline" "for fancy line editing"
