depends  zlib    &&
depends  perl    &&
depends  procps  &&
depends  g++     &&

optional_depends openssl \
                 "--with-ssl" \
                 "--without-ssl" \
                 "for OpenSSL support" &&

if list_find "$MADB_ENGINES" "all" || list_find "$MADB_ENGINES" "sphinx"; then
  depends sphinx "--with-plugin-sphinx"
fi
