depends  zlib    &&
depends  perl    &&
depends  procps  &&
depends  g++     &&

if  ! list_find "$MSQL_ENGINES" "none"  &&  list_find "$MSQL_ENGINES" "BerkeleyDB"
then
  depends  db  "--with-berkeley-db"
fi
