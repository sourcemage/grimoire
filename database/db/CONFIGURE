config_query_option  DB_JSUP                                                 \
                     "Enable Java support (e.g. to compile OpenOffice)?"  n  \
                     "--enable-java"  "--disable-java"                       &&

config_query_option  DB_TESTS                                                \
                     "Enable tests?"                                      n  \
                     "--enable-test"  "--disable-test"
