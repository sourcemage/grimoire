depends libdbi		&&
optional_depends "firebird" \
                 "--with-firebird --with-firebird-dir=/usr/firebird" \
                 "--without-firebird" \
                 "database support"  &&
optional_depends "postgresql" \
                 "--with-pgsql" \
                 "--without-pgsql" \
                 "database support" &&
optional_depends "mysql" \
                 "--with-mysql" \
                 "--without-mysql" \
                 "database support" &&
optional_depends "sqlite2" \
                 "--with-sqlite" \
                 "--without-sqlite" \
                 "database support"  &&
optional_depends "sqlite" \
                 "--with-sqlite3" \
                 "--without-sqlite3" \
                 "database support"

