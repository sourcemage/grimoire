depends -sub CXX gcc &&

if [[ $SQLITE_VERSION == tea ]]; then
  depends tcl
fi &&

optional_depends readline \
                 "--enable-readline" \
                 "--disable-readline" \
                 "for Readline support"
