./compile.sh install       &&
if spell_ok g++; then
  cd odkda-cpp             &&
  ./compile.sh install     &&
  cd ..
fi                         &&
if spell_ok mono; then
  cd odkda-sharp           &&
  ./compile.sh install     &&
  cd ..
fi

