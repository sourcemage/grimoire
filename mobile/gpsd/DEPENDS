optional_depends dbus \
                 '--enable-dbus' '--disable-dbus' \
                 "Enable dbus support"  &&

optional_depends  xorg-server \
                 '--with-x' '--without-x' \
                 "Enable X11 support"
