if false;then
DEST=$INSTALL_ROOT/sbin &&
install -d $DEST &&
install  bin/$SPELL $DEST/$SPELL
if spell_ok systemd;then
  DEST=$INSTALL_ROOT/lib/systemd/system &&
  install -d $DEST &&
  install systemd-udev/ipp-usb.service
fi


fi
make install
