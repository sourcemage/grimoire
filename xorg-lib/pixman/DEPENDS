optional_depends gtk+2 \
                 "--enable-gtk" \
                 "--disable-gtk" \
                 "To build demos - can cause circular dep build issues" &&

optional_depends libpng \
                 "--enable-libpng" \
                 "--disable-libpng" \
                 "for PNG support"
