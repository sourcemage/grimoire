depends xorgproto &&
depends AWK &&
depends libpthread-stubs &&
depends libtool &&
depends libx11 &&
depends libxau &&
depends libxcb &&
depends libxdmcp &&
depends libxext &&
depends libxt &&
depends PKG-CONFIG &&
depends util-macros &&

optional_depends gzip \
                 '--disable-open-zfile' \
                 'compression support' \
                 'to search for files with .gz and extension automatically' &&
optional_depends ncompress \
                 '--enable-stat-zfile' \
                 '--disable-stat-zfile' \
                 'to search for files with .Z and extension automatically'
