2011-07-30 Florian Franzmann <siflfran@hawo.stw.uni-erlangen.de>
	* DEPENDS: changed dependency on g++ to dependency on gcc with
	  sub-depends on CXX (scripted)

2009-01-29 Juuso Alasuutari <iuso@sourcemage.org>
	* DETAILS: Updated to 0.9.32.
	* DEPENDS: Added depends libsndfile, added optional_depends xdialog
	  and gtk+2 for GUI options, changed depends g++ to only be used
	  with gtk+2 GUI, added depends pkgconfig for gtk+2 GUI.
	* PRE_BUILD: Sedit Makefile to use sorcery CFLAGS, sedit Makefile
	  to alter GUI build/install with respect to dependencies chosen.
	* DEPENDS: Added optional_depends meterbridge.

2008-11-30 Juuso Alasuutari <iuso@sourcemage.org>
	* DETAILS, DEPENDS, PRE_BUILD, BUILD: Created spell.
