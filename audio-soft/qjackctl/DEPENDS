depends  autoconf  &&
depends  libx11    &&
depends  libxext   &&
depends -sub CXX gcc       &&
depends  qt4       &&

if [[ $QJCTL_VCS == y ]]
then
  depends  autoconf  &&
  depends  CVS
fi  &&

depends  JACK-DRIVER  &&

optional_depends  alsa-lib              \
                  "--enable-alsa-seq"   \
                  "--disable-alsa-seq"  \
                  "for ALSA/MIDI sequencer support"
