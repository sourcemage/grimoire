default_pre_build        &&
cd "${SOURCE_DIRECTORY}" &&

patch -p1 < "${SPELL_DIRECTORY}/0001-use-LDFLAGS-when-linking.patch" &&
patch -p1 < "${SPELL_DIRECTORY}/0001-Fix-undefined-behaviour-due-to-wrong-call-to-open.patch"

