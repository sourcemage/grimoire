default_pre_build                                &&
cd $SOURCE_DIRECTORY                             &&
sed -i -r '18d' Makefile                         &&
sed -i -r 's:MAN_DIR=/usr/:MAN_DIR=/usr/share/:' \
Makefile
