# Disable until openexr compiles with gcc 4.x
#config_query_option SYNFIG_EXRHALF "Enable OpenEXR's half type for color" n "--enable-half" "disable-half"  &&

config_query_option SYNFIG_G5 'Enable Mac G5 optimizations' n     \
                              '--enable-g5opt' '--disable-g5opt'  &&
config_query_list SYNFIG_OPT "Select optimization level (default 2)?" 2 0 1 3 4
