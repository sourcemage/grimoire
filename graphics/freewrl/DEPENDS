depends OPENGL       &&
depends freetype2    &&
depends libpng       &&
depends JPEG         &&
depends perl         &&
depends imagemagick  &&
depends gzip         &&
depends sox          &&
depends xorg-server  &&
depends xterm        &&


if [ "$FREEWRL_PLUGIN" == "y" ]; then
   depends GECKO
fi  &&

if [ "$FREEWRL_JAVA" == "y" ]; then
   depends JAVA
fi
