depends OPENGL       &&
depends freetype2    &&
depends libpng       &&
depends jpeg         &&
depends perl         &&
depends imagemagick  &&
depends gzip         &&
depends sox          &&
depends xorg-server   &&

. $GRIMOIRE/FUNCTIONS  &&
if check_if_xorg_modular_server; then
   depends xterm
fi                   &&

if [ "$FREEWRL_PLUGIN" == "y" ]; then
   depends GECKO
fi  &&

if [ "$FREEWRL_JAVA" == "y" ]; then
   depends JAVA
fi 

