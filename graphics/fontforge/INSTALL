default_install                                                             &&
install  -d  -m 0755  -o root  -g root  $INSTALL_ROOT/usr/share/doc/$SPELL  &&
pushd  $INSTALL_ROOT/usr/share/doc/fontforge                                &&
unpack_file  '2'                                                            &&
popd
