default_pre_build                 &&
cd  ${SOURCE_DIRECTORY}           &&
rm -f COPYING config.{guess,sub}  &&
intltoolize --force --copy        &&
libtoolize --copy --force         &&
aclocal                           &&
autoheader                        &&
automake --add-missing --copy     &&
autoconf                          &&
automake
