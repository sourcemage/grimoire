default_pre_build                               &&
cd $SOURCE_DIRECTORY                            &&
if is_depends_enabled $SPELL curl && [[ $GIMP_DEVEL != y ]]; then
   patch -p1 < $SPELL_DIRECTORY/curl.patch
fi
