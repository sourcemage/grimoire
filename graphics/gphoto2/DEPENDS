depends libgphoto2 &&
depends JPEG &&
depends libexif &&
depends popt &&

optional_depends aalib \
                 "--with-aalib" \
                 "--without-aalib" \
                 "to enable previewing" &&

optional_depends cdk \
                 "--with-cdk" \
                 "--without-cdk" \
                 "for camera dialog configuration"
