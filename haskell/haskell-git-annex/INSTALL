cd ${SOURCE_DIRECTORY} &&
default_install        &&
ln -sfn /usr/bin/git-annex "${INSTALL_ROOT}/usr/bin/git-annex-shell" &&
cd "${SOURCE_DIRECTORY}/man" &&
install -m644 *.1.gz "${INSTALL_ROOT}/usr/share/man/man1/"
