local base_lib_dir=lib
if [[ "${SMGL_COMPAT_ARCHS[1]}" == "x86_64" ]]
then
base_lib_dir=lib64
fi
mkdir  ${INSTALL_ROOT}/usr/lib/libGL-indirect                       &&
mv -fv ${SOURCE_DIRECTORY}/$base_lib_dir/* ${INSTALL_ROOT}/usr/lib/libGL-indirect

