. "$GRIMOIRE/FUNCTIONS" &&

default_pre_build &&
cd "${SOURCE_DIRECTORY}" &&

if spell_ok curl && is_version_less $(installed_version curl) 7.87.0; then
  echo "#define CURL_WRITEFUNC_ERROR 0xFFFFFFFF" >> src/update_feed/update_feed.h
fi
