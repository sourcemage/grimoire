depends pidgin    &&
depends gcc       &&
depends glib2     &&
depends zlib      &&
depends libgcrypt &&
depends gettext   &&

optional_depends libwebp \
                 "--enable-libwebp" \
                 "--disable-libwebp" \
                 "for displaying stickers" &&

optional_depends libpng \
                 "--enable-libpng" \
                 "--disable-libpng" \
                 "for converting stickers"


