default_pre_build &&

cd $SOURCE_DIRECTORY &&

patch -p0 < $SCRIPT_DIRECTORY/ayttm-libproxy.patch &&

if [ "$CVS" == "y" ]; then
      CD $SOURCE_DIRECTORY &&
      patch -p0 < $SCRIPT_DIRECTORY/fixinstall.diff &&
      ./gen
fi
