mkdir -p ${INSTALL_ROOT}/lib/security                                &&
cp pam_mysql.so ${INSTALL_ROOT}/lib/security/                        &&
mkdir -p  ${INSTALL_ROOT}/usr/share/doc/pam_mysql/                   &&
cp Changelog CREDITS Readme ${INSTALL_ROOT}/usr/share/doc/pam_mysql/
