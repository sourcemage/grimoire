depends  autoconf  &&
depends  cracklib  &&
depends  gnupg     &&
depends  smgl-fhs  &&

if [ "${PAM_SELINUX}" == "y" ]; then
  depends  libselinux  &&
  depends  gcc34
fi  &&

if [ "${PAM_DOCS}" == "y" ]; then
  depends  tetex           &&
  depends  linuxdoc-tools
fi
