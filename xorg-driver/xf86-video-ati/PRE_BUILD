default_pre_build                     &&
cd $SOURCE_DIRECTORY                  &&
patch -p1 < "${SPELL_DIRECTORY}"/radeon_dri2-bug45937.patch &&
autoreconf &&

if [[ $ATI_SCM == y ]] ; then
  sedit "s:.*configure.*::" autogen.sh  &&
  ./autogen.sh
fi
