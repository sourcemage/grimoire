depends -sub "RADEON" libdrm                 &&
depends util-macros                          &&
depends xineramaproto                        &&
depends xproto                               &&
depends xorg-server                          &&
depends -sub DRI mesalib                     &&

if [[ "$ATI_SCM" = y ]]; then
  depends git &&
  optional_depends  glamor  \
                    '--enable-glamor'  \
                    '--disable-glamor'  \
                    'for experimental 2D acceleration'
fi &&

optional_depends  UDEV  \
                  '--enable-udev'  \
                  '--disable-udev'  \
                  'for libudev hardware detection'
