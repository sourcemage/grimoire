depends glib2 &&
depends libgsf &&
depends xml-parser-expat &&
depends cairo            &&

optional_depends gtk+2 \
                 "--with-gtk" \
                 "--without-gtk" \
                 "build the UI?" &&

optional_depends gtk-doc \
                 "--enable-gtk-doc" \
                 "--disable-gtk-doc" \
                 "build gtk-doc files?"
