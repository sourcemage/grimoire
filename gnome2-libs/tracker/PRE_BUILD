default_pre_build                                 &&
cd ${SOURCE_DIRECTORY}                            &&
if [[ $TRACKER_SCM == y ]] ; then
NOCONFIGURE=X ./autogen.sh
fi                                                &&
patch -p0 < ${SCRIPT_DIRECTORY}/inotify-fix.diff
