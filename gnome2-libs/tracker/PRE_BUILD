default_pre_build                                 &&
cd ${SOURCE_DIRECTORY}                            &&
if [[ $TRACKER_SCM == y ]] ; then
NOCONFIGURE=X ./autogen.sh
else
patch -p1 < $SCRIPT_DIRECTORY/tracker-gmime-2.4.patch
fi
