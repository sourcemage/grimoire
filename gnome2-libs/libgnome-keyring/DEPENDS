depends glib2     &&
depends dbus      &&
depends libgcrypt &&
depends intltool  &&
depends libgpg-error &&

optional_depends vala \
                 --enable-vala \
		 --disable-vala \
		 "for Vala bindings" &&

optional_depends gtk-doc                  \
                 "--enable-gtk-doc"       \
                 "--disable-gtk-doc"      \
                 "to build documentation"
