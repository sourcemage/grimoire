default_pre_build &&
cd "$SOURCE_DIRECTORY" &&

if ! is_depends_enabled $SPELL gtk-doc; then
  cat "$SPELL_DIRECTORY/fake-gtk-doc-check.m4" >> acinclude.m4
fi &&

if ! spell_ok g++; then
  aclocal &&
  autoconf
fi
