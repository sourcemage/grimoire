default_pre_build &&
cd "$SOURCE_DIRECTORY" &&

if ! is_depends_enabled $SPELL gtk-doc; then
  cat "$SPELL_DIRECTORY/fake-gtk-doc-check.m4" >> acinclude.m4 &&
  echo "EXTRA_DIST=" > gtk-doc.make
fi &&

if ! spell_ok g++; then
  aclocal &&
  autoconf
fi &&

# compile with libtool 2.4
autoreconf -i
