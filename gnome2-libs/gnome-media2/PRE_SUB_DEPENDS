case $THIS_SUB_DEPENDS in
  PROFILES) if [[ $GMEDIA_GMP == "--enable-profiles" ]]; then return 0; fi ;;
         *) message "${PROBLEM_COLOR}bogus sub_depends: $SPELL $THIS_SUB_DEPENDS${DEFAULT_COLOR}" ;
  return 1;;
esac
