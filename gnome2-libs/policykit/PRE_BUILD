default_pre_build &&
cd $SOURCE_DIRECTORY &&
patch -p1 < $SPELL_DIRECTORY/bugs.freedesktop.org-16131.patch &&
sedit 's#^profiledir .*#profiledir = $(sysconfdir)/bash_completion.d#' tools/Makefile.am &&
sedit 's#^profiledir .*#profiledir = $(sysconfdir)/bash_completion.d#' tools/Makefile.in
