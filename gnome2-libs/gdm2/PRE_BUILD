default_pre_build                             &&
cd $SOURCE_DIRECTORY                          &&
patch -p0 < $SCRIPT_DIRECTORY/verify-shadow.patch  &&
cd ${SOURCE_DIRECTORY}/config                 &&
cp -f ${SCRIPT_DIRECTORY}/pam.d/* ./          

