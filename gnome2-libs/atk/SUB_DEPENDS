case "${THIS_SUB_DEPENDS}" in
  ATK_DEVEL) message "${MESSAGE_COLOR}Devel release needed, forcing it.${DEFAULT_COLOR}" &&
             ATK_DEVEL="y" ;;
          *) message "${PROBLEM_COLOR}Unknown subdepends!${DEFAULT_COLOR}" && return 1 ;;
esac
