depends devicekit &&
depends libusb    &&
depends policykit &&
depends -sub EXTRAS udev &&

optional_depends libxslt               \
                 "--enable-man-pages"  \
                 "--disable-man-pages" \
                 "Build man pages?"    &&

optional_depends gtk-doc                    \
                 "--enable-gtk-doc"         \
                 "--disable-gtk-doc"        \
                 "Build Devhelp files?"     
