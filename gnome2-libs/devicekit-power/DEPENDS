depends devicekit &&
depends libusb    &&
depends policykit &&
depends -sub GUDEV udev &&

if [[ $DEVKIT_POWER_MAN == y ]]; then
  depends libxslt  &&
  depends docbook-xsl
fi &&

optional_depends gtk-doc                    \
                 "--enable-gtk-doc"         \
                 "--disable-gtk-doc"        \
                 "Build Devhelp files?"     
