config_query CLEARLOOKS "Disable Clearlooks engine and theme?" n     &&
config_query CRUX       "Disable Crux engine and theme?" n           &&
config_query HC         "Disable High Contrast engine and theme?" n  &&
config_query INDUSTRIAL "Disable Industrial engine and theme?" n     &&
config_query LIGHTHOUSE "Disable Lighthouseblue engine and theme?" n &&
config_query METAL      "Disable Metal engine and theme?" n          &&
config_query MIST       "Disable Mist engine and theme?" n           &&
config_query REDMOND    "Disable Redmond engine and theme?" n        &&
config_query SMOOTH     "Disable Smooth engine and theme?" n         &&
config_query THINICE    "Disable Thinice engine and theme?" n        
