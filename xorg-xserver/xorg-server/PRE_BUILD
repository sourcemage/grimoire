default_pre_build                                               &&
unpack_file 2                                                   &&
cd ${SOURCE_DIRECTORY}                                          &&
patch -p0 < ${SCRIPT_DIRECTORY}/xorg-server-1.0.1-geteuid.diff  &&
patch -p1 < ${SCRIPT_DIRECTORY}/xorg-server-1.0.x-mitri.diff    &&
cd ${SOURCE_DIRECTORY}/Xext                                     &&
patch -p0 < ${SCRIPT_DIRECTORY}/fontcache.c.includes.patch
