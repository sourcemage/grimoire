diff -Nuar xorg-server-1.5.3.orig.orig/hw/xfree86/dixmods/Makefile.in xorg-server-1.5.3.orig/hw/xfree86/dixmods/Makefile.in
--- xorg-server-1.5.3.orig.orig/hw/xfree86/dixmods/Makefile.in	2009-02-08 22:58:50.000000000 +0000
+++ xorg-server-1.5.3.orig/hw/xfree86/dixmods/Makefile.in	2009-02-08 23:49:07.000000000 +0000
@@ -512,14 +512,12 @@
 module_LTLIBRARIES = $(AFBMOD) \
                      $(CFBMOD) \
                      libfb.la \
-                     libwfb.la \
                      $(MFBMOD) \
                      libshadow.la
 
 extsmoduledir = $(moduledir)/extensions
 extsmodule_LTLIBRARIES = $(RECORDMOD) \
 			 $(DBEMOD) \
-                         $(GLXMODS) \
                          $(XTRAPMOD)
 
 fontsmoduledir = $(moduledir)/fonts
@@ -612,6 +610,14 @@
 install-extsmoduleLTLIBRARIES: $(extsmodule_LTLIBRARIES)
 	@$(NORMAL_INSTALL)
 	test -z "$(extsmoduledir)" || $(MKDIR_P) "$(DESTDIR)$(extsmoduledir)"
+	$(MKDIR_P) "$(DESTDIR)$(extsmoduledir)/mesa"
+	@lst='$(GLXMODS)'; for r in $$lst; do \
+	  if test -f $$r; then \
+	    h=$(am__strip_dir) \
+	    echo " $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(extsmoduleLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) '$$r' '$(DESTDIR)$(extsmoduledir)/mesa/$$h'"; \
+	    $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(extsmoduleLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) "$$r" "$(DESTDIR)$(extsmoduledir)/mesa/$$h"; \
+	  else :; fi; \
+	done
 	@list='$(extsmodule_LTLIBRARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    f=$(am__strip_dir) \
@@ -666,6 +672,14 @@
 install-moduleLTLIBRARIES: $(module_LTLIBRARIES)
 	@$(NORMAL_INSTALL)
 	test -z "$(moduledir)" || $(MKDIR_P) "$(DESTDIR)$(moduledir)"
+	$(MKDIR_P) "$(DESTDIR)$(moduledir)/mesa"
+	@lst='libwfb.la'; for r in $$lst; do \
+	  if test -f $$r; then \
+	    h=$(am__strip_dir) \
+	    echo " $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(moduleLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) '$$r' '$(DESTDIR)$(moduledir)/mesa/$$h'"; \
+	    $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(moduleLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) "$$r" "$(DESTDIR)$(moduledir)/mesa/$$h"; \
+	  else :; fi; \
+	done
 	@list='$(module_LTLIBRARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    f=$(am__strip_dir) \
@@ -990,7 +1004,7 @@
 	done
 check-am: all-am
 check: check-recursive
-all-am: Makefile $(LTLIBRARIES)
+all-am: Makefile $(LTLIBRARIES) libwfb.la $(GLXMODS)
 installdirs: installdirs-recursive
 installdirs-am:
 	for dir in "$(DESTDIR)$(extsmoduledir)" "$(DESTDIR)$(fontsmoduledir)" "$(DESTDIR)$(moduledir)"; do \
diff -Nuar xorg-server-1.5.3.orig.orig/hw/xfree86/dri/Makefile.in xorg-server-1.5.3.orig/hw/xfree86/dri/Makefile.in
--- xorg-server-1.5.3.orig.orig/hw/xfree86/dri/Makefile.in	2009-02-08 22:58:50.000000000 +0000
+++ xorg-server-1.5.3.orig/hw/xfree86/dri/Makefile.in	2009-02-08 22:59:46.000000000 +0000
@@ -408,7 +408,7 @@
                    @GL_CFLAGS@
 
 libdri_la_LDFLAGS = -module -avoid-version @LIBDRM_LIBS@
-libdri_ladir = $(moduledir)/extensions
+libdri_ladir = $(moduledir)/extensions/mesa
 libdri_la_SOURCES = \
 	dri.c \
 	dri.h \
