default_pre_build          &&
cd $SOURCE_DIRECTORY 	   &&
patch  -p0 < $SPELL_DIRECTORY/post-3.5.7-kdebase-konqueror.diff  &&
# splashscreen patch
if [[ $SMGL_THEME == "y" ]]; then
  unpack_file 2
fi
