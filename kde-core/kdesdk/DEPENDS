depends  kdebase  &&

source  $SECTION_DIRECTORY/KDE_DEPENDS  &&

optional_depends  db                            \
                  ''                            \
                  ''                            \
                  'for Berkeley DB IV support'  &&

if  [  "$KDESDK_SUBVERSION"  ==  "y"  ];  then
  depends  APU         &&
  depends  subversion
fi
