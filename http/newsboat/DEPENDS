. "${GRIMOIRE}/FUNCTIONS" &&

depends -sub CXX gcc &&
depends asciidoc &&
depends curl &&
depends gettext &&
depends json-c &&
depends libxml2 &&
depends pkgconfig &&
depends rust &&
depends sqlite &&
depends stfl &&

if spell_ok gcc && is_version_less $(installed_version gcc) 4.8; then
  depends -sub REGEX boost
fi
