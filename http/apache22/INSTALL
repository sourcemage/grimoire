create_group daemon                        &&
create_account daemon                      &&
default_install                            &&
ln -sf  $TRACK_ROOT/usr/sbin/envvars $INSTALL_ROOT/usr/bin/envvars
