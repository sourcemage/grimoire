optional_depends SSL \
                 "--enable-https" \
                 "--disable-https" \
                 "for HTTPS support" &&

optional_depends zlib \
                 "--with-zlib=${INSTALL_ROOT}/lib" \
                 "--without-zlib" \
                 "for zlib compression support"
