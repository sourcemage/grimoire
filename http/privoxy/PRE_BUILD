default_pre_build     &&
cd $SOURCE_DIRECTORY  &&
# add updated actions file
verify_file 2         &&
cp $SOURCE_CACHE/$SOURCE2 ./default.action &&
# apply smgl patch
patch -p1 <$SPELL_DIRECTORY/mk.patch &&
autoheader &&
autoconf
