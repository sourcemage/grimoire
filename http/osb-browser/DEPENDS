depends pkgconfig             &&
if [[ $OSB_SCM == y ]]; then
depends subversion
fi                            &&
depends -sub CXX gcc                   &&
depends bison                 &&
depends libtool               &&
depends xorg-libs  "--with-x"  &&
depends nrcit                 &&
depends glib2

