Currently fails with:
 g++ -DHAVE_CONFIG_H -I. -I. -I.. -I.. . -I.. -Wall -I/usr/include/SDL -D_REENTRANT -I.. . -I.. -O -march=pentium4 -mmmx -mfpmath=sse -msse -msse2 -pipe -DPIC -fPIC -Os -MT config.lo -MD -MP -MF .deps/config.Tpo -c config.cpp  -fPIC -DPIC -o .libs/config.o
g++: .: linker input file unused because linking not done
g++: .: linker input file unused because linking not done
cc1plus: .: No such file or directory
make[2]: *** [config.lo] Error 1
make[2]: Leaving directory `/usr/src/gnash-0.7/libbase'


GECKO providers don't work as expected, currently the configure checks for firefox-config, then for mozilla-config, so if only seamonkey is installed, the plugin won't be built.

fails to use libxml2 correctly here if built by the spell, a manual configure --enable-xml --enable-xmlreader works
