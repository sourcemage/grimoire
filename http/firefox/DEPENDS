depends  g++                           &&
depends  zlib    '--with-system-zlib'  &&
depends  jpeg    '--with-system-jpeg'  &&
depends  libpng  '--with-system-png'   &&
depends  zip                           &&
depends  perl                          &&
depends  nspr    '--with-system-nspr'  &&

optional_depends nss '--enable-crypto' '--disable-crypto' 'for encrypted communications (https://)'  &&

if  [  "$FIREFOX_GTK"  =  "2.x"  ];  then
  depends  gtk+2 '--enable-default-toolkit=gtk2 --enable-xft --disable-freetype2' &&    # doesn't work
  depends libidl
else
#
#  Else  FIREFOX_GTK="1.x"
#
  depends  gtk+  '--enable-default-toolkit=gtk'  &&
  depends  orbit
fi  &&

if  [  "$FIREFOX_CVS"  =  "y"  ];  then
  depends  CVS
fi
