message  "${PROBLEM_COLOR}If ${SPELL_COLOR}firefox${PROBLEM_COLOR} does not"  \
         "properly work with find in page for you then try removing"          \
         "~/.mozilla/firefox/<profile>/compreg.dat${DEFAULT_COLOR}"
