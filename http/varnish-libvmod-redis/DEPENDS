depends varnish &&
depends hiredis &&
depends libev &&

optional_depends SSL \
                 "--enable-tls" \
                 "--disable-tls" \
                 "for SSL/TLS support" &&

optional_depends docutils "" "" "to generate manual page"
