if [[ $NEWSBEUTER_BRANCH == scm ]]; then
 depends git
fi &&

depends sqlite &&
depends curl &&
depends gettext &&
depends pkgconfig &&
depends libxml2 &&
depends -sub CXX gcc &&
depends stfl &&
depends ncurses &&
depends openssl &&
depends json-c
