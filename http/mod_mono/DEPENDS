depends mono &&

if   spell_ok apache2  ; then depends apache2  ;
elif spell_ok httpd-dev; then depends httpd-dev;
else
  message "${PROBLEM_COLOR}Apache 2 must be present as one of spells: apache2 or httpd-dev${DEFAULT_COLOR}"
  return
fi
