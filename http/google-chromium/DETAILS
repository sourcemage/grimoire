           SPELL=google-chromium
if  [[ "$GOOGLE_CHROME_SCM_AUTOUPDATE" == "y" ]];  then
         VERSION=$(date +%Y%m%d)
else
         VERSION=svn
fi
          SOURCE=$SPELL-svn.tar.bz2
   SOURCE_IGNORE=volatile
SOURCE_DIRECTORY=$BUILD_DIRECTORY/$SPELL-svn
   SOURCE_URL[0]=svn_http://src.chromium.org/svn/trunk/src:$SPELL
      LICENSE[0]=BSD
        WEB_SITE=http://chrome.google.com/
        KEYWORDS="web http"
         ENTERED=20100105
           SHORT='Google web browser'
cat << EOF
Google Chrome is a web browser that runs web pages and applications with
lightning speed.
EOF
