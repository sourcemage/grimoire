make    install                                    &&
make    install-help                               &&
make    install-doc                                &&
ln    -sf  ../var/lib/lynx/lynx.cfg $INSTALL_ROOT/etc/lynx.cfg  &&
rm    -f  $INSTALL_ROOT/usr/bin/lynx.old
