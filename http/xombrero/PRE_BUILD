default_pre_build &&
cd $SOURCE_DIRECTORY &&

patch -p1 < $SPELL_DIRECTORY/xombrero-TAILQ_FOREACH_SAFE.patch &&

sed -i "s:/local::" linux/Makefile &&
sed -i 's:/usr/local/share/xombrero/:/usr/share/xombrero/:' xombrero.conf unix.c
