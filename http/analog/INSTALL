cp analog ${INSTALL_ROOT}/usr/bin  &&

if ! [ -d ${INSTALL_ROOT}/usr/lib/analog ];
then
    mkdir ${INSTALL_ROOT}/usr/lib/analog
fi

if ! [ -d ${INSTALL_ROOT}/usr/lib/analog/lang ];
then
    mkdir ${INSTALL_ROOT}/usr/lib/analog/lang
fi

cp -rf lang/* ${INSTALL_ROOT}/usr/lib/analog/lang



