depends  daemon   &&
optional_depends pcre "" "" "regular expression support" &&
optional_depends openssl \
                 "--disable-tls" \
                 "--enable-tls=openssl" \
                 "support secure connections"
