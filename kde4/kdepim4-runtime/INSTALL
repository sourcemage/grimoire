default_install &&
if [[ "${KDE_VER}" == "unstable" ]];then
# install missing header files
  DEST=$INSTALL_ROOT/opt/kde4/include/akonadi &&
  mkdir -p $DEST  &&
  cp $SOURCE_DIRECTORY/akonadi_next/entitytreeview.h $DEST  &&
  cp $SOURCE_DIRECTORY/akonadi_next/akonadi_next_export.h $DEST
fi
