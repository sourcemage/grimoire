default_pre_build    &&
cd $SOURCE_DIRECTORY &&
if [[ "${KDE_VER}" == "unstable" ]];then
# temporary patch - 4.3.68
  patch  -p3 < $SPELL_DIRECTORY/katescriptview.h
fi  &&
sed -i '/update_xdg_mimetypes/D' mimetypes/CMakeLists.txt
