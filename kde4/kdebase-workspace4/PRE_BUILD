create_group  kde4    &&
default_pre_build &&
patch -p1 -d $SOURCE_DIRECTORY < $SPELL_DIRECTORY/sensors-fix.patch &&
patch -p1 -d $SOURCE_DIRECTORY < $SPELL_DIRECTORY/shutdown-fix.patch &&
patch -p0 -d $SOURCE_DIRECTORY < $SPELL_DIRECTORY/terminate-server.patch
