persistent_read kdelibs4 KDE_VER KDE_VER  &&
if [[ "${KDE_VER}" == "stable" ]];then
  message ${PROBLEM_COLOR}this spell is not available for stable kde4${DEFAULT_COLOR}
  return 1
else
  return 0
fi
