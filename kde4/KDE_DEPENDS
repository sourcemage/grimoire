source  $GRIMOIRE/FUNCTIONS            &&
depends g++      &&
depends cmake    &&
case $SPELL in
  kdelibs4)   depends qt4              &&
              if check_if_xorg_modular_libs  qt4  ;  then
                depends  iceauth       &&
                depends  libice        &&
                depends  libsm         &&
                depends  libxext
              fi                       &&
              depends dbus             &&
              depends hal              &&
              depends pkgconfig        &&
              depends openssl          &&
              sub_depends qt4 DBUS     &&
              sub_depends qt4 SSL      &&
              depends strigi           &&
              sub_depends strigi QT4   &&
              depends shared-mime-info &&
              depends  perl            &&
              depends  pcre            &&
              depends  libpng          &&
              depends libxml2          &&
              depends libxslt          &&
              depends LIBGIF           &&
              depends FAM              &&
              depends soprano          &&
              depends clucene          

              ;;

  kdepimlibs4) depends kdelibs4        &&
               depends gpgme           &&
               depends libgpg-error    &&
               depends boost
               ;;
  
  kdebase-workspace4)
               depends kdepimlibs4
               ;;

  kdebase4)    depends kdepimlibs4     &&
               depends qimageblitz
               ;;

  *)           depends KDEBASE4
               ;;
esac
