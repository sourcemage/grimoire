source  $GRIMOIRE/KDE_DEPENDS &&
if is_depends_enabled $SPELL qt6base;then
  depends qt6tools
else
  depends qttools &&
  optional_depends qtxmlpatterns '' '' 'Compile-time validation of syntax definition files'
fi


