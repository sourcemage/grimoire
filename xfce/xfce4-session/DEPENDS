depends iceauth &&
depends xml-parser-expat &&
depends dbus-glib &&
depends libxfce4ui &&
depends libwnck &&
depends xfconf &&

optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support" &&

optional_depends upower \
                 "--enable-upower" \
                 "--disable-upower" \
                 "for D-Bus and PolicyKit power device enumeration" &&

optional_depends hal \
                 "--enable-hal" \
                 "--disable-hal" \
                 "for HAL support" &&

optional_depends consolekit \
                 "--enable-consolekit" \
                 "--disable-consolekit" \
                 "for ConsoleKit support" &&

optional_depends policykit \
                 "--enable-polkit" \
                 "--disable-polkit" \
                 "for PolicyKit support" &&

optional_depends xfce4-panel \
                 "--enable-panel-plugin" \
                 "--disable-panel-plugin" \
                 "for Panel logout menu button" &&

optional_depends gnome-keyring \
                 "--enable-libgnome-keyring" \
                 "--disable-libgnome-keyring" \
                 "for gnome-keyring compatibility support" &&

optional_depends gnome-doc-utils \
                 "--enable-gen-doc" \
                 "--disable-gen-doc" \
                 "to generate HTML documentation" &&

optional_depends gconf2 \
                 "--enable-gnome" \
                 "--disable-gnome" \
                 "for Assistive technologies support"
