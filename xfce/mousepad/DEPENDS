depends glib2 &&
depends gtk+2 &&
depends gtksourceview &&

optional_depends dbus-glib \
                 "--enable-dbus" \
                 "--disable-dbus" \
                 "for D-BUS support" &&

optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
