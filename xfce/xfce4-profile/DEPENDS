depends X11-LIBS           &&
depends gtk+2              &&
depends libxml2            &&
depends libxfce4util       &&
depends libxfce4mcs        &&
depends libxfcegui4        &&
depends xfce-mcs-manager   &&
depends xfdesktop          &&
depends xfce4-panel        &&
depends xfwm4              &&
depends xfce-utils         &&
depends xfce4-session      &&
depends hicolor-icon-theme &&

suggest_depends xfce-mcs-plugins "" "" \
                "for multi channel settings manager" &&

suggest_depends gtk-xfce-engine "" "" \
                "for gtk2 theme engine" &&

suggest_depends mousepad "" "" \
                "for a text editor" &&

suggest_depends orage "" "" \
                "for a calendar application" &&

suggest_depends squeeze "" "" \
                "for a graphical archiver" &&

suggest_depends terminal "" "" \
                "for xfce terminal" &&

suggest_depends thunar "" "" \
                "for xfce file manager" &&

suggest_depends thunar-archive-plugin "" "" \
                "for Thunar Archive context plugin" &&

suggest_depends thunar-media-tags-plugin "" "" \
                "for Thunar media tags plugin" &&

suggest_depends thunar-thumbnailers "" "" \
                "for Thunar thumbnails plugin" &&

suggest_depends thunar-volman "" "" \
                "for a media Thunar volume manager" &&

suggest_depends verve-plugin "" "" \
                "for a command launcher panel plugin" &&

suggest_depends xarchiver "" "" \
                "for a graphical archiver" &&

suggest_depends xfce4-appfinder "" "" \
                "for menu generation of apps that don't add themselves" &&

suggest_depends xfce4-artwork "" "" \
                "for additional backgrounds" &&

suggest_depends xfce4-battery-plugin "" "" \
                "for a battery monitor panel plugin" &&

suggest_depends xfce4-clipman-plugin "" "" \
                "for a clipboard manager panel plugin" &&

suggest_depends xfce4-cpugraph-plugin "" "" \
                "for a different systemload display" &&

suggest_depends xfce4-cpufreq-plugin "" "" \
                "for a CPU Frequency panel plugin" &&

suggest_depends xfce4-dict-plugin "" "" \
                "for Dictionary plugin" &&

suggest_depends xfce4-datetime-plugin "" "" \
                "for a date/time and calendar panel plugin" &&

suggest_depends xfce4-diskperf-plugin "" "" \
                "for a disk performance monitor panel plugin" &&

suggest_depends xfce4-eyes-plugin "" "" \
                "for following eyes" &&

suggest_depends xfce4-fsguard-plugin "" "" \
                "for a disc space monitor panel plugin" &&

suggest_depends xfce4-icon-theme "" "" \
                "for the xfce4 rodent icon theme" &&

suggest_depends xfce4-iconbox "" "" \
                "for an iconbox" &&

suggest_depends xfce4-mixer "" "" \
                "for a mixer for the xfce4 panel" &&

suggest_depends xfce4-mpc-plugin "" "" \
                "for MPD panel plugin" &&

suggest_depends xfce4-netload-plugin "" "" \
                "for a network load panel plugin" &&

suggest_depends xfce4-notes-plugin "" "" \
                "for a sticky notes panel plugin" &&

suggest_depends xfce4-places-plugin "" "" \
                "for a GNOME Places panel plugin" &&

suggest_depends xfce4-systemload-plugin "" "" \
                "for a system load panel plugin" &&

suggest_depends xfce4-wavelan-plugin "" "" \
                "for a WaveLAN panel plugin" &&

suggest_depends xfce4-weather-plugin "" "" \
                "for a weather display" &&

suggest_depends xfce4-xkb-plugin "" "" \
                "for a keyboard layout switcher/display" &&

suggest_depends xfce4-xmms-plugin "" "" \
                "for a xmms control panel plugin" &&

suggest_depends xfprint "" "" \
                "for a print dialog/printer manager" &&

suggest_depends xfwm4-themes "" "" \
                "for additional xfwm4 themes" &&

suggest_depends xfmedia "" "" \
                "for a XINE-based lightweight media player" &&

suggest_depends xfburn "" "" \
                "for a simple burning tool GUI"
