if [[ "$XFCE_POLKIT_BRANCH" == "scm" ]]; then
  . "${GRIMOIRE}/MESON_DEPENDS"
fi &&
depends PKG-CONFIG            &&
depends glib2                 &&
depends libxfce4ui            &&
depends policykit
