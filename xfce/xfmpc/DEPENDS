depends glib2 &&
depends gtk+2 &&
depends libxfce4util &&
depends libxfcegui4 &&
depends libmpd &&

optional_depends GETTEXT \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
