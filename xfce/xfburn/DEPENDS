depends  CD-BURN-UTILITY  &&
depends  cdrdao           &&
depends  exo              &&
depends  glib2            &&
depends  gtk+2            &&
depends  intltool         &&
depends  libxfcegui4      &&

if [ "$XFBURN_SVN" == "y" ]; then
  depends  subversion
fi                       &&

optional_depends  thunar                    \
                  "--enable-thunar-vfs"     \
                  "--disable-thunar-vfs"    \
                  "for Thunar's VFS layer"
