depends glib2 &&
depends dbus-glib &&
depends libxfce4util &&

optional_depends perl-glib \
                 "--enable-perl-bindings" \
                 "--disable-perl-bindings" \
                 "for Perl bindings"
