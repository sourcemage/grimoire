depends MESON-BUILD &&

# prefer embed samurai if available
if [[ $(get_spell_provider ${SPELL} MESON-BUILD) != muon ]] || muon version | grep -vq "^enabled features:.*samurai"; then
  depends NINJA-BUILD
fi
