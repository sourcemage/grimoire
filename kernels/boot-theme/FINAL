message  "${MESSAGE_COLOR}Remember to add \"initrd=/boot/initrd.splash\" to your bootloader${DEFAULT_COLOR}"

#####################
# We should probably update the bootloader configs after this but there needs
# to be an official script to do that for every bootloader out there and for
# multiple spells to use and potentially the user (an installed script would be
# nice to do this).
#####################
