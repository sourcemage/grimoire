cd $INSTALL_ROOT/lib/modules/$(uname -r)/kernel/drivers/char/drm/  &&
local i                                                            &&
for i in *.ko
do
  mv -fv $i $i.bak
done                                                               &&
default_pre_install

