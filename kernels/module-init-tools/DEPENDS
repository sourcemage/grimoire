depends  flex      &&
depends  gnupg     &&
depends  smgl-fhs  &&
depends  docbook2x &&

if [ "$OLD_TOO" == "y" ]; then
  depends  bison
fi  &&

if [ "$GZIP_MODS" == "y" ] ; then
  depends  zlib  '--enable-zlib'
fi
