depends  which             &&
depends  module-init-tools &&
optional_depends libselinux "USE_SELINUX=true" "USE_SELINUX=false" "For selinux support" &&
optional_depends SYSTEM-LOGGER "USE_LOG=true" "USE_LOG=false" "For logging through syslog"
