#
# Copy all of the themes to /etc/splash
#
for theme in `ls`;  do
  #
  # Remove 'fbsplash-' from the theme name when installing
  #
  cp  -r  $theme  $INSTALL_ROOT/etc/splash/${theme/fbsplash-}
done  &&

#
# This pops up and gets tracked...
#
rm  -rf  $INSTALL_ROOT/etc/splash/old.libraries
