todo
----
xen 3.0.2 version

xen 2 doesn't work with twisted in test, stable is fine

option to modify grub

doc stuff (needs transgif which needs X)

optional_depends (lvm, nfs, etc?)

bridge-utils isn't really needed but xen checks and won't build without it

restore existing config and/or add oldtree option and/or allow selection of
kernel make type or integrate with linux spell for kernel building

done
----
menuconfig doesn't work: it can't run from BUILD, but that's where it is in the build process for xen

init.d scripts

xen 3 version
