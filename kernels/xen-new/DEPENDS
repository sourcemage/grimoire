depends  grub       &&
depends  iproute2   &&
depends  zlib       &&
depends  python     &&

if  [  ${XEN_2}  ==  "y"  ]  ;  then
  depends  curl     &&
  depends  twisted
else
  depends  udev
fi  &&

# need to figure out if this can be made optional, xen only needs it in certain
# configurations but the build looks for it
depends  bridge-utils
