conflicts  xen         &&
conflicts  xen-kernel # &&

# not strictly required, but see bug #10940.  also, for now let's make sure
# people get that they're replacing their core system kernel.
# but not yet.
#conflicts  linux
