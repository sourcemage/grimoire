source $GRIMOIRE/depends_one_of.function &&
depends JPEG   &&
depends libpng &&
depends zlib   &&
optional_depends_one_of HTML_CUPS \
               libcups '' \
               cups '' \
               '--without-http' \
              'CUPS HTTP/HTTPS support' &&

optional_depends    llvm '' '' 'use CLang' &&

optional_depends    "fltk"                          \
                    "--with-gui=yes"                \
                    "--with-gui=no"                 \
                    "for compile with GUI"
