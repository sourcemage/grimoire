config_query FBREADER_SCM "Do you want SCM version" "n" &&
if [[ "${FBREADER_SCM}" == "y" ]]
then
config_query FBREADER_SCM_AUTO \
          "Automaticaly update the spell on sorcery queue/system-update?" \
          "n"
fi
