depends  expat &&
depends  enca &&
depends  liblinebreak &&
depends  fribidi &&
if [[ $UI == gtk2 ]]; then
  depends gtk+2
fi &&
depends sqlite &&
if [[ $FBREADER_SCM == y ]];then
  depends subversion
fi
