make install &&
make install-man &&

ln -sfn /usr/bin/openjade /usr/bin/jade                       &&
ln -sfn /usr/lib/libogrove.so /usr/lib/libgrove.so            &&
ln -sfn /usr/lib/libospgrove.so /usr/lib/libspgrove.so        &&
ln -sfn /usr/lib/libostyle.so /usr/lib/libstyle.so            &&

install -m 644 dsssl/catalog /usr/share/sgml/OpenJade         &&
install -m 644 dsssl/*.{dtd,dsl,sgm} /usr/share/sgml/OpenJade 
