default_pre_build                                               &&
cd $SOURCE_DIRECTORY                                            &&
patch -p1 < $SCRIPT_DIRECTORY/sgml-common-0.6.3-manpage-1.patch &&
aclocal                                                         &&
automake -acf                                                   &&
autoconf

