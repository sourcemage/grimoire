install -m 755 bin/collateindex.pl ${INSTALL_ROOT}/usr/bin                                 &&
install -m 644 bin/collateindex.pl.1 ${INSTALL_ROOT}/usr/share/man/man1                    &&

install -d -m 755 ${INSTALL_ROOT}/usr/share/sgml/docbook/dsssl-stylesheets/${VERSION}      &&

cp -R * /usr/share/sgml/docbook/dsssl-stylesheets/${VERSION}
