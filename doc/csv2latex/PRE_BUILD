mk_source_dir   $SOURCE_DIRECTORY               &&
unpack_file                &&

mv  $SOURCE_CACHE/$SOURCE  $SOURCE_DIRECTORY    &&

cd      $SOURCE_DIRECTORY                       &&
gunzip  $SOURCE
