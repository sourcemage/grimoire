mk_source_dir   $SOURCE_DIRECTORY                           &&
cd              $SOURCE_DIRECTORY                           &&

cp              ${SOURCE_CACHE}/$SOURCE $SOURCE_DIRECTORY   &&
bunzip2         $SOURCE
