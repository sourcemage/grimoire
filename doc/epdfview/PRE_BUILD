default_pre_build &&
cd "$SOURCE_DIRECTORY" &&

patch -p0 < "$SPELL_DIRECTORY/mouse-wheel.patch" &&
sed -e "s:poppler_page_get_text:poppler_page_get_selected_text:" \
    -e "s:page, &textRect:page, POPPLER_SELECTION_GLYPH, \&textRect:" -i src/PDFDocument.cxx
