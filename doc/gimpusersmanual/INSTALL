local   INSTDIR=${INSTALL_ROOT}/usr/share/doc/$SPELL        &&

mkdir   --parents   $INSTDIR                                &&

install --mode=644  ${SOURCE_CACHE}/$SOURCE     $INSTDIR    &&
install --mode=644  ${SOURCE_CACHE}/$SOURCE2    $INSTDIR
