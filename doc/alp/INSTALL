local INSTDIR=${INSTALL_ROOT}/usr/share/doc/$SPELL  &&
mkdir --parents "$INSTDIR"                          &&
cd "$SOURCE_DIRECTORY"                              &&
cp           $SOURCE      "$INSTDIR"                &&
cp --archive ALP-listings "$INSTDIR"
