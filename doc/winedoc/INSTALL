local INSTDIR=${INSTALL_ROOT}/usr/share/doc/$SPELL      &&

install --directory     $INSTDIR                        &&

install -m 644  ${SOURCE_CACHE}/$SOURCE     $INSTDIR    &&
install -m 644  ${SOURCE_CACHE}/$SOURCE2    $INSTDIR    &&
install -m 644  ${SOURCE_CACHE}/$SOURCE3    $INSTDIR
