default_pre_build                       &&

if [ "$CSTEX" = "y" ]; then
    cd  $SOURCE_DIRECTORY/texk/web2c    &&
    unpack_file 2
fi  &&

cd  $SOURCE_DIRECTORY                             &&
patch -p1 < $SCRIPT_DIRECTORY/glibc-2.10.1.patch
