default_pre_build &&
cd $SOURCE_DIRECTORY &&

patch -p1 < $SPELL_DIRECTORY/mc-4.6.1-bash-all.patch &&

if [[ $MC_UNICODE == y ]]; then
  bunzip2 -c $SPELL_DIRECTORY/mc-4.6.1-utf8-all.patch.bz2 | patch -p1
fi
