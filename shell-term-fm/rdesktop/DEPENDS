optional_depends  "openssl"                                     \
                  "--with-openssl=$INSTALL_ROOT/usr"            \
                  ""                                            \
                  "use openssl instead of static own binaries"
