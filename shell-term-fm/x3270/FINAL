   message "${PROBLEM_COLOR}Fonts will properly function only after${DEFAULT_COLOR}" 
   message "${PROBLEM_COLOR}successful execution of the${FILE_COLOR}" \
           "xset fp rehash${PROBLEM_COLOR} command.${DEFAULT_COLOR}"
