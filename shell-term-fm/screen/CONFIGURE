config_query SCREEN_256COLOR "Enable 256 color support?" n &&
config_query SCREEN_RC "Install the Source Mage /etc/screenrc ?" n &&
config_query SCREEN_SUID "Install screen binary suid-root (for multiuser support)?" n
