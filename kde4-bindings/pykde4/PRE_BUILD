default_pre_build &&
cd $SOURCE_DIRECTORY &&
# phonon is not installed in / not in /opt/kde4 that is kde4 include dir...
sedit "s|\${KDE4_INCLUDE_DIR}/phonon|$INSTALL_ROOT/usr/include/phonon|" CMakeLists.txt
