default_pre_build        &&
cd  "$SOURCE_DIRECTORY"  &&

sed -i  "s|/usr/local|/usr|"  ${_SRCDIR}linux/Makefile  &&

if [[ $JACKDMP_TMPDIR && $JACKDMP_TMPDIR != '/dev/shm' ]]
then
  sed -i  "s|/dev/shm|$JACKDMP_TMPDIR|"  ${_SRCDIR}common/JackConstants.h
fi
