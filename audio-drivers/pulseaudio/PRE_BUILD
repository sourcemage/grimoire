default_pre_build &&
cd "$SOURCE_DIRECTORY" &&
message "${MESSAGE_COLOR}Fixing dbus build in Makefile.in.$DEFAULT_COLOR" &&
sed -i -e 's/^\(libdbus_util_la_LIBADD = .*\)HAL_LIBS/\1DBUS_LIBS/'       \
       -e 's/^\(libdbus_util_la_CFLAGS = .*\)HAL_CFLAGS/\1DBUS_CFLAGS/'   \
  src/Makefile.in
