depends coreutils &&
depends alsa-lib &&
depends xmlto &&

if [[ $ALSA_UTILS_GIT == y ]]; then
  depends git &&
  depends libtool &&
  depends automake &&
  depends autoconf
fi &&

runtime_depends which
