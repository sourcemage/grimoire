depends coreutils &&
depends alsa-lib &&

optional_depends xmlto \
                 "--enable-xmlto" \
                 "--disable-xmlto" \
                 "for generated documentation" &&

if [[ $ALSA_UTILS_GIT == y ]]; then
  depends git &&
  depends libtool &&
  depends automake &&
  depends autoconf
fi &&

runtime_depends which
