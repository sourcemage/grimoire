if [[ $ALSA_LIB_GIT == y ]]; then
  depends git &&
  depends libtool &&
  depends automake &&
  depends autoconf
fi &&

optional_depends python \
                 "--enable-python" \
                 "--disable-python" \
                 "for python components"
