depends xorg-server &&
depends rman       &&

if [[ "$(get_spell_provider xorg-server)" == "xorg-server" ]]; then
  depends gccmakedep &&
  depends libxt &&
  depends libsm &&
  depends libice &&
  depends libxext &&
  depends libx11 &&
  depends libxpm &&
  depends libxp &&
  depends libxmu &&
  depends libxaw &&
  depends imake
fi

