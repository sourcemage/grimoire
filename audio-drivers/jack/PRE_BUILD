default_pre_build        &&
cd  "$SOURCE_DIRECTORY"  &&

if [[ $JACK_VCS == y ]]
then
  ./autogen.sh --no-conf
fi  &&

if [[ $JACK_TIME == y ]]
then
  patch -p1 < "$SCRIPT_DIRECTORY/jack_time.patch"
fi
