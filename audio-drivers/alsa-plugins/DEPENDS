depends  alsa-lib  &&
if [[ $ALSA_PLUG _GIT == y ]]; then
  depends git &&
  depends libtool &&
  depends automake &&
  depends autoconf
fi  &&

optional_depends  LIBAVCODEC               \
                  ""                       \
                  ""                       \
                  "for a52 plugin"         &&

optional_depends  JACK-DRIVER              \
                  ""                       \
                  ""                       \
                  "for jack plugin"        &&

optional_depends  libsamplerate            \
                  ""                       \
                  ""                       \
                  "for samplerate plugin"  &&

optional_depends  pulseaudio               \
                  ""                       \
                  ""                       \
                  "for pulseaudio plugin"
