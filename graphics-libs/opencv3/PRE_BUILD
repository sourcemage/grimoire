default_pre_build &&
cd $SOURCE_DIRECTORY &&
if ! list_find "$CV_CONTRIB" none ;then
 mkdir contrib &&
 cd contrib    &&
 for FF in $CV_CONTRIB;do
   ln -sf $BUILD_DIRECTORY/opencv_contrib/modules/$FF
 done
fi
