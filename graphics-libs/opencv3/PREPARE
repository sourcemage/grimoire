source $GRIMOIRE/config_query_multi.function
config_query_multi CV_CONTRIB "Which contributed modules" none face xphoto
message "contributed module not currently supported"
if ! list_find "$CV_CONTRIB" none ;then
  message "${PROBLEM_COLOR}contributed module not currently supported${MESSAGE_COLOR}"
  false
fi
