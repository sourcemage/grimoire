default_pre_build                          &&
cd $SOURCE_DIRECTORY                       &&
patch -p1 < $SCRIPT_DIRECTORY/gcc43.patch  &&
sedit 's: lua < 5.1::' acinclude.m4        &&
touch INSTALL NEWS                         &&
autoreconf
