mk_source_dir  ${SOURCE_DIRECTORY}    &&
cd  ${BUILD_DIRECTORY}                &&
unpack_file          &&
unpack_file 2         &&
unpack_file 3         &&
unpack_file 4         &&
cd  ${SOURCE_DIRECTORY}
