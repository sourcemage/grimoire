./configure  --build=$BUILD                                   \
            --prefix=${INSTALL_ROOT}/usr                      \
             --docdir=${INSTALL_ROOT}/usr/share/doc/${SPELL}  \
                $OPTS                          &&
make
                                                                 
