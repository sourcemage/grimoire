depends  OPENGL      &&
if test mesalib-1x = "$(get_spell_provider $SPELL OPENGL)"; then
  # New mesalib has it separated out.
  depends  glu
fi &&
depends  libx11      &&
depends  libxi       &&
depends  libxext     &&
depends  libxxf86vm  &&
depends  libxrandr
