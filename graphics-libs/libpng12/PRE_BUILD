default_pre_build &&
cd "$SOURCE_DIRECTORY" &&
sedit '
      # disable symlinks conflicting with libpng spell
      /^\t\$(MAKE)/{/install-....-hook$/d}
      # do not install manpages
      /^install-data-am:/{s/ install-man / /}
' Makefile.in
