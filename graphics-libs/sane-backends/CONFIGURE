config_query USEFORK "Use fork instead of pthread?" y &&
config_query IPV6 "Enable IPV6 support?" y &&
config_query HP5400NVSM "Disable Version String Matching in HP5400 driver (needed on certain models)?" n


