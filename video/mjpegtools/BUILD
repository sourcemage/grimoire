# fails to compile with -DPIC and -fPIC (except on all-PIC archs)
disable_pic &&

make_single                                                   &&
default_build                                                 &&
make_normal
