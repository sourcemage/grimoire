if ! (get_spell_provider "${SPELL}" "LIBAVCODEC" 2> /dev/null); then
  check_tmp_noexec /tmp 
fi &&

default_build &&

if ! (get_spell_provider "${SPELL}" "LIBAVCODEC" 2> /dev/null); then
  check_tmp_noexec /tmp  
fi
