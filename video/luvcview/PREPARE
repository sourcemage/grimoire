config_query LUVCVIEW_SCM "Build SCM (subversion) version?" n &&

if [ "$LUVCVIEW_SCM" == "y" ]; then
  config_query LUVCVIEW_SCM_AUTOUPDATE                         \
               "Automatically update on every system update?"  n
fi
