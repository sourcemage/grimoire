default_pre_build     &&
cd $SOURCE_DIRECTORY  &&

if [ "${KINO_CVS}" == 'n' ]; then
   patch -p0 < ${SCRIPT_DIRECTORY}/frame.cc.patch
elif [ "${KINO_CVS}" == 'y' ]; then
   NOCONFIGURE=Y ./autogen.sh
fi
