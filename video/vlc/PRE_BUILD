default_pre_build      &&
cd  $SOURCE_DIRECTORY  &&
patch  -p0  <  $SCRIPT_DIRECTORY/mozilla-plugin.patch  &&
patch  -p0  <  $SCRIPT_DIRECTORY/vlc-wx-unicode.patch  &&
# fix png linking
sedit  's/-lpng -lz/-lpng -lm -lz/'  configure
