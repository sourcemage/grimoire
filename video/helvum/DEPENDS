. "${GRIMOIRE}/MESON_DEPENDS" &&

if [[ "${HELVUM_BRANCH}" == "scm" ]]; then
  depends git
fi &&

depends appstream-glib &&
depends glib2 &&
depends gtk+4 &&
depends  -sub "CLANG" llvm &&
depends pipewire &&
depends rust
