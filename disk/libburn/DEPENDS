depends pkgconfig &&

if [[ $LIBBURN_SVN == y ]]; then
  depends libtool &&
  depends automake &&
  depends autoconf &&
  depends subversion
fi &&

suggest_depends libisoburn "" "" "for an ISO generation tool"
