depends pkgconfig &&

if [[ $LIBBURN_BRANCH == scm ]]; then
  depends libtool &&
  depends automake &&
  depends autoconf &&
  depends subversion
fi &&

suggest_depends libisoburn "" "" "for an ISO generation tool"
