default_pre_build         &&

cd "$SOURCE_DIRECTORY"    &&

if [ "$FUSE_BRANCH" != "scm" ]; then
  patch -p0 < "$SPELL_DIRECTORY/init.patch"
fi  &&

# glibc-2.14 fix
sed -i "/sched/s|<|<linux/|" util/fusermount.c &&

if [ "$FUSE_BRANCH" = "scm" ]; then
  ./makeconf.sh
fi
