default_pre_build         &&

cd "$SOURCE_DIRECTORY"    &&

patch -p0 < "$SPELL_DIRECTORY/init.patch" &&

# glibc-2.14 fix
sed -i "/sched/s|<|<linux/|" util/fusermount.c &&

if [ "$CVS" = "y" ]; then
  ./makeconf.sh
fi
