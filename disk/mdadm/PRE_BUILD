default_pre_build            &&
cd ${SOURCE_DIRECTORY}       &&
sedit 's:-Wall::' Makefile   &&
sedit 's:-Wextra::' Makefile &&
sedit 's:-Werror::' Makefile &&
patch -p1 -d $SOURCE_DIRECTORY < $SPELL_DIRECTORY/mdadm-3.3.2-gcc5-1.patch
