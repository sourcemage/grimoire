default_pre_build                                               &&
cd ${SOURCE_DIRECTORY}                                          &&
if spell_installed dvdrtools; then
  patch -p1 < $SPELL_DIRECTORY/dvdrtools.patch
fi
