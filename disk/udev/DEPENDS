depends gnupg &&
depends smgl-fhs &&

optional_depends libselinux \
                 "USE_SELINUX=true" \
                 "USE_SELINUX=false" \
                 "for SELinux support" &&

optional_depends SYSTEM-LOGGER \
                 "USE_LOG=true" \
                 "USE_LOG=false" \
                 "for logging to syslog"
