depends  util-linux  &&

optional_depends pkgconfig \
                 "--enable-pkgconfig" \
                 "" \
                 "to install pkgconfig support" &&

optional_depends readline \
                 "--enable-readline" \
                 "--enable-readline=no" \
                 "for readline shell support"
