default_install  &&
if [ "$CDRECORD_SYMLINK" = "y" ]; then
  ln -s $TRACK_ROOT/usr/bin/wodim $INSTALL_ROOT/usr/bin/cdrecord
  ln -s $TRACK_ROOT/usr/bin/readom $INSTALL_ROOT/usr/bin/readcd
  ln -s $TRACK_ROOT/usr/bin/genisoimage $INSTALL_ROOT/usr/bin/mkisofs
fi
