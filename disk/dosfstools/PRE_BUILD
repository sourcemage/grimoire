default_pre_build                                   &&
cd $SOURCE_DIRECTORY                                &&
sedit "s:PREFIX =:PREFIX = $INSTALL_ROOT:" Makefile &&
sedit "s:/usr/man:/usr/share/man:" Makefile
