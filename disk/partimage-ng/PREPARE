if spell_ok $SPELL;then
  if [[ `installed_version $SPELL` != bzr ]];then
    message ${PROBLEM_COLOR}Beware - archive format has changed -old backups will be unusable!${DEFAULT_COLOR}
  fi
fi &&
config_query PART_VCS 'use BZR version?' y

