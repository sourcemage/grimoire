if [[ $PART_VCS == y ]];then
  depends bzr           &&
  depends boost         &&
  depends $PART_NTFS    &&
  depends pkgconfig     &&
  optional_depends hal  '' '-DNO_HAL=1' 'CD detection'
else 
  depends ntfsprogs
fi &&
depends cmake           &&
depends g++             &&
depends parted          &&
depends e2fsprogs

