depends autoconf &&
depends attr &&
depends dmapi &&

optional_depends gettext \
                 "--enable-gettext" \
                 "--disable-gettext" \
                 "for alternate language support"
