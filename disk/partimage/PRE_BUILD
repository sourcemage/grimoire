default_pre_build                       &&
cd $SOURCE_DIRECTORY			&&
sedit "/partimag.root/D" Makefile.in  	&&
#  ./autogen.sh                         &&
#  touch mkinstalldirs                  &&
echo "mkdir -p \$1" > ./mkinstalldirs 	&&
chmod u+x ./mkinstalldirs
