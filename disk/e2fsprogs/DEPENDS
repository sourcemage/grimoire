depends AWK &&
depends INITSCRIPTS &&
depends smgl-fhs &&
depends pkgconfig &&

optional_depends GETTEXT \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
