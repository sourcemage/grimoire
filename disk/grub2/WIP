It default installs which doesn't conflict with the old grub
but it does install grub-install so this might confuse people

# gaze from grub-install
grub-0.97:/sbin/grub-install
grub2-1.93:/usr/sbin/grub-install

And it's certainly not been tested as a replacement for grub yet
