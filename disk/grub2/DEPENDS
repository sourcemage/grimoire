depends  automake  &&
depends  gnupg     &&
depends  smgl-fhs  &&
depends  lzo       &&

if [[ "${GRUB2_SVN}" = 'y' ]]; then
  depends autoconf &&
  depends subversion &&
  depends ruby
fi  &&

optional_depends  freetype2                  \
                  '--enable-grub-mkfont'     \
                  '--disable-grub-mkfont'    \
                  'for grub-mkfont utility'
