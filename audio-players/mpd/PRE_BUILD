default_pre_build  &&
cd  ${SOURCE_DIRECTORY}  &&

if  [[  $INIT_INSTALLED  ]]; then
     create_account musicdaemon                               &&
     sedit "s:/etc/mpd.conf:/etc/sysconfig/mpd:g" src/main.c
fi
