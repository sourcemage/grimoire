default_pre_build &&
cd "$SOURCE_DIRECTORY" &&
apply_patch_dir patches &&

# Unbundle libintl (causes build issues)
cat > intl/Makefile.in << EOF &&
all:
install: all
EOF
sed -E -i '/^(LT)?LIBINTL *=/{s/=.*/=/}' src/Makefile.in
