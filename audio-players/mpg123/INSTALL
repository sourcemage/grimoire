cd "$SOURCE_DIRECTORY" &&
if [[ ${SMGL_COMPAT_ARCHS[0]} == 32 ]]; then
  cp -av mpg123-smallfile/$INSTALL_ROOT/usr/lib/libmpg123* "$INSTALL_ROOT/usr/lib" &&
  cp -av mpg123-smallfile/$INSTALL_ROOT/usr/lib/pkgconfig/libmpg123.pc "$INSTALL_ROOT/usr/lib/pkgconfig"
fi &&
default_install
