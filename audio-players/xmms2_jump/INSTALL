cp -v xmms2_jump-{makedb,monitor,user_query.sh} \
      $INSTALL_ROOT/usr/bin  &&
if [[ -e xmms2_jump-query-gtk ]] ; then
cp -v xmms2_jump-query-gtk $INSTALL_ROOT/usr/bin
fi
if [[ -e xmms2_jump-query-curses ]] ; then
cp -v xmms2_jump-query-curses $INSTALL_ROOT/usr/bin
fi
