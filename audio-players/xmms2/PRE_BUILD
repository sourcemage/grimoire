mk_source_dir $SOURCE_DIRECTORY  &&
cd $SOURCE_DIRECTORY             &&
unpack_file                      &&
cd xmms*                         &&
echo -e "#!/bin/sh\ncd xmms*\n./waf configure --help" > ../configure  &&
chmod +x ../configure                                          &&
patch -p0 < $SPELL_DIRECTORY/fix_flags.diff
