cd  $SOURCE_DIRECTORY/xmms*                 &&
scons MANDIR=${INSTALL_ROOT}/usr/share/man  \
      PREFIX=${INSTALL_ROOT}/usr            \
      CCFLAGS="$CFLAGS"                     \
      CXXFLAGS="$CXXFLAGS"                  \
      EXCLUDE="$OPTS"                       &&
scons MANDIR=${INSTALL_ROOT}/usr/share/man  \
      PREFIX=${INSTALL_ROOT}/usr            \
      CCFLAGS="$CFLAGS"                     \
      CXXFLAGS="$CXXFLAGS"                  \
      EXCLUDE="$OPTS"
