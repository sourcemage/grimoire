depends ncurses &&
optional_depends curl \
                 "--with-curl" \
                 "--without-curl" \
                 "for fetching lyrics from the Internet" &&
optional_depends taglib \
                 "--with-taglib" \
                 "--without-taglib" \
                 "enable tag editor" 
