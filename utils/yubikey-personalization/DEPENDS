depends automake &&
depends autoconf &&

depends yubico-c &&
depends libusb   &&
optional_depends json-c \
                 "--with-json" \
                 "--without-json" \
                 "for JSON YCFG support"
