default_install                      &&
cd $SOURCE_DIRECTORY                 &&
DEST=${INSTALL_ROOT}/etc/profile.d   &&
mkdir  -p  ${DEST}                   &&
install  -m  0644  $SCRIPT_DIRECTORY/strigi.sh  ${DEST}
