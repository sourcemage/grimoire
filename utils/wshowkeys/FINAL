# wshowkeys must be configured as setuid during installation.
# It requires root permissions to read input events.
# These permissions are dropped after startup.
chmod a+s "${INSTALL_ROOT}/usr/bin/wshowkeys"
