default_pre_build                                       &&
cd $SOURCE_DIRECTORY                                    &&
sedit '
  s:^DESTDIR ?=:DESTDIR ?= $INSTALL_ROOT:
  s:/usr/man:/usr/share/man:
  s:cp -fpR:cp -fR:
' Makefile
