default_pre_build                                                             &&
cd $SOURCE_DIRECTORY && patch -p1 < ${SCRIPT_DIRECTORY}/mx518.patch           &&
cd $SOURCE_DIRECTORY && patch -p1 < ${SCRIPT_DIRECTORY}/verbose-report.patch
