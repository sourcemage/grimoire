runtime_depends AWK &&
runtime_depends git &&

suggest_depends GNUPG "" "" "for encryption support via gnupg" &&
suggest_depends SSL "" "" "for encryption support via openssl" &&
suggest_depends git-crypt "" "" "for encryption support via git-crypt"
