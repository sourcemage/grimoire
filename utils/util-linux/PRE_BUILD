default_pre_build &&
cd $SOURCE_DIRECTORY &&

if [[ $UL_LAES == y ]]; then
  bzcat $SPELL_DIRECTORY/loop-aes-util-linux-ng-2.13.1.diff.bz2 | patch -p1
fi &&

if [[ $SELINUX == y ]]; then
  OPTS="--with-selinux"
fi
