#
# This is an example /etc/filesystems (See mount(8)) provided by
# Source Mage GNU/Linux
#
# Line seperated options with 'nodev' meaning 'don't try this type'
#

#ext3            # Trye ext3 before ext2
#ext2
#nodev proc      # Don't try procps
#nodev devpts    # Don't try devpts
#iso9660
#udf
#reiserfs
#jfs
#xfs
#vfat            # Try vfat before msdos so that we can have nice
#msdos           # filenames on floppies (usually loads msdos first :()
#*               Continue searching in /proc/filesystems
