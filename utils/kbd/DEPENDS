depends gnupg &&
depends smgl-fhs &&
depends autoconf &&
depends automake &&

optional_depends linux-pam \
                 "--enable-vlock" \
                 "--disable-vlock" \
                 "build vlock" &&
optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
