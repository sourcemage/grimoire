depends gnupg &&
depends smgl-fhs &&
depends autoconf &&

optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
