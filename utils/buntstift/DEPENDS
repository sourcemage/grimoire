depends perl &&
depends config-param &&
message "Plain script generation is still possible without gnuplot..." &&
suggest_depends gnuplot "" "" "for actual plotting" &&
suggest_depends text-numericdata "" "" "for parsing data files; generate labels"
