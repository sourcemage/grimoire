depends glib2 &&
depends gtk+2 &&
depends xorg-libs &&
depends pkgconfig &&

. $GRIMOIRE/FUNCTIONS
if check_if_xorg_modular_libs; then
  depends libxrender &&
  depends libxv "--with-xv-path=$INSTALL_ROOT/usr/lib/" &&
  depends libxxf86vm &&
  depends xf86vidmodeproto
fi
