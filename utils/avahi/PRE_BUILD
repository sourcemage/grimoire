default_pre_build                          &&
cd $SOURCE_DIRECTORY 	                   &&
patch  -p1 < $SPELL_DIRECTORY/qt.patch     &&
patch  -p2 < $SCRIPT_DIRECTORY/dbus.patch  &&
autoreconf                                 &&
create_group  netdev
