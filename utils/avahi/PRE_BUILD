default_pre_build                          &&
cd $SOURCE_DIRECTORY 	                   &&
patch  -p1 < $SPELL_DIRECTORY/qt.patch     &&
autoreconf                                 &&
create_group  netdev                       &&
create_group  avahi
