--- Makefile.in.orig	2009-03-15 13:57:01.000000000 +0300
+++ Makefile.in	2009-03-26 02:45:56.593257418 +0300
@@ -732,12 +732,16 @@
 install-sysconfDATA: $(sysconf_DATA)
 	@$(NORMAL_INSTALL)
 	test -z "$(sysconfdir)" || $(MKDIR_P) "$(DESTDIR)$(sysconfdir)"
-	@list='$(sysconf_DATA)'; for p in $$list; do \
-	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
-	  f=$(am__strip_dir) \
-	  echo " $(sysconfDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(sysconfdir)/$$f'"; \
-	  $(sysconfDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(sysconfdir)/$$f"; \
-	done
+	@if [ ! -f $(DESTDIR)$(sysconfdir)/metalog.conf ]; then \
+	  @list='$(sysconf_DATA)'; for p in $$list; do \
+	    if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
+	    f=$(am__strip_dir) \
+	    echo " $(sysconfDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(sysconfdir)/$$f'"; \
+	    $(sysconfDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(sysconfdir)/$$f"; \
+	  done \
+	else \
+	    echo "$(DESTDIR)$(sysconfdir)/metalog.conf already exists, install will not overwrite"; \
+	fi
 
 uninstall-sysconfDATA:
 	@$(NORMAL_UNINSTALL)
