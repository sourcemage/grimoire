optional_depends linux-pam \
                 "--enable-pam=yes" \
                 "--enable-pam=no" \
                 "for PAM support" &&

optional_depends libselinux \
                 "--with-selinux" \
                 "--without-selinux" \
                 "for SELinux support" &&

optional_depends audit \
                 "--with-audit" \
                 "--without-audit" \
                 "for audit trails"
