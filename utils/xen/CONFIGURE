config_query XEN_HVM "Do you want to build HVM (hardware-assisted virtualization) support" y &&
config_query XEN_STUBDOM "Do you want to include Device Model Stub Domains? support" y &&

config_query XEN_UPINIT "Do you want to install upstream init scripts" y
