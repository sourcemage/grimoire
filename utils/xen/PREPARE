config_query_list XEN_BRANCH "Which branch of $SPELL do you want to build?" \
                             4.4 \
                             4.2 &&

if [[ -z "$XEN_BRANCH" ]]; then
  XEN_BRANCH="4.4"
fi
