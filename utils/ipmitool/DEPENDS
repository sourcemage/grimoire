depends ncurses &&
depends readline &&

optional_depends openssl \
                 "--disable-internal-md5" \
                 "--enable-internal-md5" \
                 "to use external MD5 library"
