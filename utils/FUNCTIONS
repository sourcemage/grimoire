cmake_build() {
cd $SOURCE_DIRECTORY  &&
mkdir -p build        &&
cd build              &&
cmake -DCMAKE_INSTALL_PREFIX=$INSTALL_ROOT/usr ../  $OPTS &&
make
}
qt4_build() {
QTDIR=/usr
PATH=$QTDIR/bin/qt4:$PATH
cmake_build
}
