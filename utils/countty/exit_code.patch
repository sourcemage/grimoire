--- countty.c.orig
+++ countty.c
@@ -282,5 +282,6 @@ main(int argc, char **argv)
         wait_for_next_second(sync_fraction);
     }
 
-    exit(EXIT_SUCCESS);
+    /* Aborted by SIGINT. */
+    exit(EXIT_FAILURE);
 }
