mk_source_dir ${SOURCE_DIRECTORY} &&
cd  $SOURCE_DIRECTORY             &&
cp $SOURCE_CACHE/${SOURCE} ${SOURCE_DIRECTORY}/colorgcc    &&
cp $SOURCE_CACHE/${SOURCE2} ${SOURCE_DIRECTORY}/colorgccrc &&
sedit  "s:local/::"  colorgcc
