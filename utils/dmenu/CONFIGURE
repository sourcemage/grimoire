persistent_remove DMENU_XFT &&

message "dmenu does not use a config file, you might want to customize it now" &&
message "Your custom config will be saved to ${INSTALL_ROOT}/usr/share/doc/dmenu/" &&

config_query DMENU_CONFIG "Do you wish to use a custom config.def.h?" n
