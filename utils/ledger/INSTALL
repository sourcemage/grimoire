default_install                                                     &&
mkdir -p $INSTALL_ROOT/usr/share/emacs/site-lisp                    &&
install -m0644 ledger.el ${INSTALL_ROOT}/usr/share/emacs/site-lisp
