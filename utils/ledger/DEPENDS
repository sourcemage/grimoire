depends  gmp        &&
depends  pcre       &&
depends  automake   &&
depends  autoconf   &&
depends  libtool  

if [ "$PYT" == y ]; 
then
    depends python
fi                  &&

if [ "$XML" == y ];
then
    depends libxml2
fi
