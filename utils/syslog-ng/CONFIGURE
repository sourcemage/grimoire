if  !  grep  -q  "CONF_SPECIAL_CONF="  $SPELL_CONFIG;  then

  if    query  "Install custom made syslog-ng.conf ? (if no - the default syslog-ng.conf.sample will be used)" n
  then  echo   "CONF_SPECIAL_CONF=y"  >>  $SPELL_CONFIG
  else  echo   "CONF_SPECIAL_CONF=n"  >>  $SPELL_CONFIG
  fi

fi
