depends pkgconfig &&
depends pcre &&

optional_depends zlib \
                 "--enable-zlib" \
                 "--disable-zlib" \
                 "for zlib compressed search support" &&

optional_depends xz-utils \
                 "--enable-lzma" \
                 "--disable-lzma" \
                 "for lzma compressed search support"
