depends autoconf &&
depends automake &&
depends libtool  &&

optional_depends libcap \
                 "--enable-capabilities" \
                 "--disable-capabilities" \
                 "for more system capabilities"
