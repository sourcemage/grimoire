default_pre_build &&
cd "${SOURCE_DIRECTORY}" &&
sedit '4018a\
    x11\' configure &&

if [[ "$UNITY_GRID" = 'y' ]]; then
  cd "${SOURCE_DIRECTORY}/src" &&
  patch < "${SCRIPT_DIRECTORY}/grid.patch"
fi
