FONT_DIR=${INSTALL_ROOT}/usr/share/fonts/misc           &&
mkdir -p ${FONT_DIR}                                    &&
install -m 644 -o root -g root ${SOURCE_DIRECTORY}/lfp-var/*.pcf.gz \
   ${FONT_DIR}
