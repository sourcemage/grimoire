(

  prepare_install                                             &&
  mkdir    -p  /usr/share/fonts/truetype                      &&
  install  -o root  -g root  -m 644  -p                       \
  $SOURCE_DIRECTORY/*.ttf /usr/share/fonts/truetype

) > $C_FIFO 2>&1
