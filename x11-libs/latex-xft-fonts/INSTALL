FONT_DIR="${INSTALL_ROOT}/usr/share/fonts/TTF"
mkdir    -p $FONT_DIR                                     &&
install  -o root  -g root  -m 644  -p                      \
  $SOURCE_DIRECTORY/*.ttf $FONT_DIR
