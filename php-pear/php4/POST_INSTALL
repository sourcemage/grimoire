default_post_install &&
message "${PROBLEM_COLOR}PHP 4.x has reached end-of-life and is no longer supported." &&
message "Given this, we strongly encourage to upgrade to PHP 5.x series as this spell" &&
message "will be removed in the not-so-distant future from our grimoires.${DEFAULT_COLOR}"
