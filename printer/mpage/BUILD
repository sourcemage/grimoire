(

  make  PAGESIZE=Letter  PREFIX=${INSTALL_ROOT}/usr           &&
  prepare_install                                             &&
  make  PAGESIZE=Letter  PREFIX=${INSTALL_ROOT}/usr  install

) > $C_FIFO 2>&1
