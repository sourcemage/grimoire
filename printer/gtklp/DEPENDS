depends libx11 &&
depends gtk+2 &&
depends intltool &&
depends CUPS &&

optional_depends SSL \
                 "--enable-ssl" \
                 "--disable-ssl" \
                 "for SSL/TLS support" &&

optional_depends gettext \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
