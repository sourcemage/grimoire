UPDEPS=$(fast_up_depends gnulpr 1)
if [[ ! -z $UPDEPS ]]; then
  message "${MESSAGE_COLOR}You need to recast some spells without gnulpr support:" &&
  message -n "$DEFAULT_COLOR" &&
  for each in "$UPDEPS"; do
    up_trigger $each cast_self
  done
fi
