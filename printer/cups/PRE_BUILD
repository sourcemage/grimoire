default_pre_build                               &&
cd  $SOURCE_DIRECTORY                           &&

patch  -p1 < $SCRIPT_DIRECTORY/xpdf-pl3.patch   &&
patch  -p0  <  $SCRIPT_DIRECTORY/Makefile.diff

