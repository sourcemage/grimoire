default_pre_build    &&
cd $SOURCE_DIRECTORY &&
# remove most of the embedded libraries. use the system ones instead
rm -rf freetype jpeg jpegxr lcms  libpng openjpeg tiff lcms2 cups ijs expat *.vcproj
