message "${WARNING_COLOR}PLEASE NOTE: HP is replacing hpijs with hplip for most of their devices${DEFAULT_COLOR}"
message "${WARNING_COLOR}(including printer/scanner multifunction devices), so you probably want${DEFAULT_COLOR}"
message "${WARNING_COLOR}to cast the hplip spell instead. A list of the few devices which still${DEFAULT_COLOR}"
message "${WARNING_COLOR}require hpijs is available at hpinkjet.sourceforge.net.${DEFAULT_COLOR}"
