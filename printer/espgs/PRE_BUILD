default_pre_build                        &&

if  [  "${SOURCE4}"  !=  ""  ];  then
  cd      ${SOURCE_DIRECTORY}            &&
  unpack  ${SOURCE4}  ${MD5[3]}
fi                                       
