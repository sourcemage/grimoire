if grep -q 'START_ON_BOOT=\"y\"' ${SPELL_CONFIG}; then
  if ! grep -q 'mgetty' ${INSTALL_ROOT}/etc/inittab; then
    echo $( grep 'INITTAB_STRING' ${SPELL_CONFIG} | cut -d\" -f2 ) >> ${INSTALL_ROOT}/etc/inittab
  fi
fi
