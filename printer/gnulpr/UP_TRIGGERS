UPDEPS=$(fast_up_depends cups 1)
if [[ ! -z $UPDEPS ]]; then
  message "${MESSAGE_COLOR}You need to recast some spells without cups support:" &&
  message -n "$DEFAULT_COLOR" &&
  for each in "$UPDEPS"; do
    up_trigger $each cast_self
  done
fi
