config_query XML_PP "Install xml_pp (XML pretty printer)" y
config_query XML_GREP "Install xml_grep (grep XML files using XML::Twig's subset of XPath)" y
config_query XML_SPLIT "Install xml_split (split big XML files)" y
config_query XML_MERGE "Install xml_merge (merge back files created by xml_split)" y
config_query XML_SPELLCHECK "Install xml_spellcheck (spellcheck XML files skipping tags)" y
