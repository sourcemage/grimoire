default_pre_build &&

patch -p0 < ${SPELL_DIRECTORY}/ghc-6.8.patch &&

CONFIG=${SOURCE_DIRECTORY}/Config.hs

if [ "$MY_CONFIG" == "y" ] ; then
  cp -v $MY_CONFIG_PATH $CONFIG
fi &&

if [ "$EDIT_CONFIG" == "y" ] ; then
  $EDITOR $CONFIG
fi
