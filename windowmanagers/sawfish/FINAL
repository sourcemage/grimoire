message "NOTE: ~/.sawmillrc is no longer a valid config file." &&
message "sawfish-ui is renamed to sawfish-config" &&
message "if your ~/.sawfish[/]rc contains the line:
(require 'sawmill-defaults)
then updated the line to the new value:
(require 'sawfish-defaults)
"
