depends  xorg-libs  &&
depends  tiff       &&
depends  libpng     &&
depends  libxft     &&
depends  zlib       &&
depends  jpeg       &&
depends  LIBGIF     &&

if [[ "${WINDOWMAKER_CVS}" == "y" ]] || [[ ${SMGL_COMPAT_ARCHS[1]} == x86_64 ]]; then
    depends CVS        &&
    depends libtool    &&
    depends autoconf
fi
