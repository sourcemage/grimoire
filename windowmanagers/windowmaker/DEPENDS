depends  X11-LIBS  &&
depends  tiff      &&
depends  libpng    &&
depends  zlib      &&
depends  jpeg      &&
depends  libungif  &&

if [[ "${WINDOWMAKER_CVS}" == "y" ]] ; then
    depends CVS        &&
    depends libtool    &&
    depends autoconf
fi &&


source ${GRIMOIRE}/FUNCTIONS &&
if check_if_xorg_modular_libs
then depends libxft
fi
