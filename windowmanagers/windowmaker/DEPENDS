depends  tiff      &&
depends  libpng    &&
depends  zlib      &&
depends  jpeg      &&
depends  libungif  &&

if [[ "${WINDOWMAKER_CVS}" == "y" ]] ; then
    depends CVS        &&
    depends libtool    &&
    depends autoconf
else depends gcc34
fi
