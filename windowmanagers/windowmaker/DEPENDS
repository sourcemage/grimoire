depends  tiff      &&
depends  libpng    &&
depends  zlib      &&
depends  jpeg      &&
depends  LIBGIF    &&

if [[ "${WINDOWMAKER_CVS}" == "y" ]] ; then
    depends CVS        &&
    depends libtool    &&
    depends autoconf
fi
