depends  X11-LIBS  &&
depends  autoconf  &&
depends  libtool   &&
depends  lua       &&

.  "$GRIMOIRE/FUNCTIONS"

if  check_if_xorg_modular_libs
then
  depends  libsm    &&
  depends  libx11   &&
  depends  libice   &&
  depends  libxext
fi
