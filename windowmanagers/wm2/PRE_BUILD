default_pre_build     &&
cd ${SOURCE_DIRECTORY} &&
sedit 's/\#define SIGNAL_CALLBACK_TYPE (void (\*)(\.\.\.))/\#define SIGNAL_CALLBACK_TYPE (void (\*)(int))/g' ${SOURCE_DIRECTORY}/General.h &&
sedit '6a\
#include <linux/stddef.h>\' Border.C 

