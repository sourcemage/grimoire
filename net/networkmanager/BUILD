#
# We have a choice of redhat, gentoo, debian, or slackware currently
# I think redhat is closest
#
OPTS="--with-distro=redhat  \
      LIBNL_CFLAGS=-I/usr/include/netlink LIBNL_LIBS=-lnl \
      $OPTS"                &&

default_build
