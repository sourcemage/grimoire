depends glib2 &&

optional_depends SYSTEM-LOGGER \
                 "--enable-syslog" \
                 "--disable-syslog" \
                 "for Syslog logging" &&

optional_depends zlib "" "" "to build gznbd"
