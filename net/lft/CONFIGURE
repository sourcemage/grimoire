config_query LFT_SUID_ROOT "Install $SPELL setuid root?" n &&

config_query_option LFT_OPTS "Use gettimeofday instead of pcap timestamps?" n \
                               "--enable-gtod" \
                               "--disable-gtod"
