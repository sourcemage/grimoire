if [ "$NAGIOS_HTML" == "y" ]; then
   depends WEBSERVER
   depends gd
fi  

optional_depends MAIL_TRANSPORT_AGENT    \
                 "--with-mail"           \
                 "--without-mail"        \
                 "Enable mail support?"  
