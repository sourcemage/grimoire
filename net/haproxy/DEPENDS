optional_depends zlib \
                 "USE_ZLIB=1" \
                 "USE_ZLIB=0" \
                 "for gzip/deflate compression support" &&

optional_depends SSL \
                 "USE_OPENSSL=1" \
                 "USE_OPENSSL=0" \
                 "for SSL support" &&

optional_depends pcre \
                 "USE_PCRE=1" \
                 "USE_PCRE=0" \
                 "for regex support"
