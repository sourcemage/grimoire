case $THIS_SUB_DEPENDS in
  CHECKSUM) message "iptables with CHECKSUM requested, forcing CHECKSUM patch" &&
            IPTABLES_CHECKSUM=y ;;
         *) message "${PROBLEM_COLOR}bogus sub_depends: $SPELL $THIS_SUB_DEPENDS${DEFAULT_COLOR}"
          return 1 ;;
esac
