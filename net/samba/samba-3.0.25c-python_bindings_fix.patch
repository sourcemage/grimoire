--- source/python/py_smb.c	(revision 24634)
+++ source/python/py_smb.c	(revision 24635)
@@ -48,7 +48,7 @@
 
 	ZERO_STRUCT(ip);
 
-	if (!cli_connect(cli, server, &ip))
+	if (!NT_STATUS_IS_OK(cli_connect(cli, server, &ip)))
 		return NULL;
 
 	return new_cli_state_object(cli);
