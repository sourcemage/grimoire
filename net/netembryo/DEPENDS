optional_depends lksctp-tools       \
                 "--enable-sctp"    \
                 "--disable-sctp"   \
                 "For SCTP support" &&
optional_depends openssl             \
                 "--with-openssl"    \
                 "--without-openssl" \
                 "Enable SSL support through OpenSSL"
