optional_depends lksctp-tools       \
                 "--enable-sctp"    \
                 "--disable-sctp"   \
                 "For SCTP support" &&
optional_depends SSL             \
                 "--with-opennssl"    \
                 "--without-opennssl" \
                 "Enable SSL support through OpenSSL"
