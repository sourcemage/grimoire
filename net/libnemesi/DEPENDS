depends git       &&
depends autoconf  &&
depends automake  &&
depends netembryo &&

optional_depends lksctp-tools     \
                 "--enable-sctp"  \
                 "--disable-sctp" \
                 "Enable SCTP support"
