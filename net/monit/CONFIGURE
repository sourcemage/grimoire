if ! list_find "$CFLAGS" "-O3"; then
  config_query_option MONIT_OPTS "Enable optimized (-O3) build" n \
                                 "--enable-optimized" \
                                 "--disable-optimized"
fi
