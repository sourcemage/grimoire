chmod  o+r  /var/log/snort/*

message  "The access rights of /var/log/snort/* is changed to o+r"

message  "The installed files /usr/bin/snortparse.cgi and
/usr/bin/nslookup.cgi must be reachable from the web server.
You may make softlinks to them, or simply copy them in web
server documents directory. Be sure you have ExecCGI directive."

message  "Better start snort with -m 0022 options for creating files
word readable. The web server MUST be capable of reading log files
somehow."
