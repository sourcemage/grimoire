depends SSL &&

optional_depends zlib \
                 "--enable-zlib" \
                 "--disable-zlib" \
                 "for zlib compression support" &&

optional_depends lzo \
                 "--enable-lzo" \
                 "--disable-lzo" \
                 "for lzo compression support"
