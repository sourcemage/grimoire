depends iproute2      && 
depends libnl         &&
depends hal           &&
depends gconf2        &&
depends policykit     &&
depends wpa_supplicant &&
depends -sub EXTRAS udev &&

if [[ $NETWORK_MANAGER_SCM == y ]]; then
  depends ppp &&
  depends -sub 3.12.x nss &&
  depends gtk-doc
else
  depends nss
fi
