depends autoconf &&
optional_depends net-snmp "--enable-snmp" "--disable-snmp" "For SNMP support" &&
if [[ "ZEBRA_VTYSH" == "--enable-vtysh" ]] ; then
  optional_depends  linux-pam  '--with-libpam'  '--without-libpam'  'For PAM support in vtysh'
fi
