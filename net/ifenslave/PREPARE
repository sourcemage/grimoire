. "${GRIMOIRE}/FUNCTIONS" &&

if ! spell_ok linux && [[ -x "${INSTALL_ROOT}/sbin/ifenslave" ]]; then
  message "${PROBLEM_COLOR}Manually-installed ifenslave detected${DEFAULT_COLOR}" &&

  return 1
fi
