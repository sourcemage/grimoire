depends  gcc34  &&
optional_depends  "tcp_wrappers"    \
                  "--with-libwrap"  \
                  ""                \
                  "for hosts.allow and hosts.deny support (tcp_wrappers)"
