config_query LIBNETFILTER_CONNTRACK_GIT "Build the git version?" n

if [[ "$LIBNETFILTER_CONNTRACK_GIT" == "y" ]]
then
  config_query  LIBNETFILTER_CONNTRACK_GIT_AUTOUPDATE        \
                "Automatically update $SPELL on system-update?"  n
fi
