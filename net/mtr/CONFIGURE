config_query_option MTR_OPTS \
                    "Enable IPv6?" y \
                    "--enable-ipv6" \
                    "--disable-ipv6" &&

config_query MTR_SUID "Install mtr setuid (recommended)" y
