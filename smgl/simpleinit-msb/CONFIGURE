config_query SIMPLEINIT_NLS                                          \
             "Disable NLS (advised if /usr is mounted separately)?"  \
             n                                                       &&
config_query SIMPLEINIT_STATIC                                       \
             "Build a static init?"                                  \
             n

