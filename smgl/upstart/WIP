upstart builds and installs fine.
for testing purposes, all binaries are prefixed with upstart so the simpleinit ones are not overwritten.

There are NO smgl init scripts for upstart yet, working on those.
