mkdir  $SOURCE_DIRECTORY                              &&
cd     $SOURCE_DIRECTORY                              &&
unpack_file                                           &&
patch  -p0  <  ${SCRIPT_DIRECTORY}/src2html.sh.patch
