if [ -z $(installed_version gnustep-base) ]
then
    return 0
fi                                                  &&

if [ $(installed_version gnustep-gui) != $VERSION ]
then
    RECAST_UP="gnustep-gui"
fi
