depends          gnustep-make   &&
depends          ffcall         &&
depends          libxml2        &&
optional_depends libxslt        &&
optional_depends openssl        &&
optional_depends gmp            &&

if [ "$DOCU" == "y" ]; 
then
	depends tetex           &&
	depends texinfo
fi

