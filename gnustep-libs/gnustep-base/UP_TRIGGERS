if [ -z $(installed_version gnustep-base) ]
then
    return 0
fi                                                   &&

if [ $(installed_version gnustep-base) != $VERSION ]
then
    RECAST_UP="gnustep-base"
fi
