export TAR_OPTIONS="--no-same-owner"

make_single
if [ "$APPK" == "n" ];
then
    make appkit=no GNUSTEP_INSTALLATION_DIR=$GNUSTEP_SYSTEM_ROOT install 
else
    make GNUSTEP_INSTALLATION_DIR=$GNUSTEP_SYSTEM_ROOT install
fi
make_normal
